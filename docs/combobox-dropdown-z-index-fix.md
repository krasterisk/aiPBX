# Исправление: Dropdown скрывается под формой

## 🐛 Проблема

Выпадающий список (dropdown) в Combobox скрывался под вышестоящими элементами (формами, карточками).

---

## ✅ Решение

Увеличен `z-index` для dropdown меню с `100` до `1000`.

**Файл**: `src/shared/ui/redesign-v3/Combobox/Combobox.module.scss`

**Было:**
```scss
.menu {
    z-index: var(--z-dropdown); // 100
}
```

**Стало:**
```scss
.menu {
    z-index: 1000; // Высокий z-index для отображения поверх форм
}
```

---

## 📊 Z-index иерархия

Для справки, вот типичная иерархия z-index в приложении:

| Элемент | Z-index | Описание |
|---------|---------|----------|
| Базовый контент | 0-9 | Обычные элементы |
| Sticky элементы | 10-99 | Липкие заголовки |
| **Dropdown (старый)** | **100** | ❌ Недостаточно |
| Modal backdrop | 500 | Затемнение для модалок |
| **Dropdown (новый)** | **1000** | ✅ Поверх всего |
| Modal content | 1000 | Содержимое модалок |
| Tooltips | 1500 | Подсказки |
| Toast/Notifications | 9999 | Уведомления |

---

## 🔍 Почему это произошло?

1. **Низкий z-index**: `--z-dropdown: 100` был недостаточен
2. **Stacking context**: Формы и карточки могут иметь свой контекст наложения
3. **Position: relative**: Родительские элементы создают новый контекст

---

## 💡 Альтернативные решения (не применены)

### 1. Portal (React)
```tsx
// Рендерить dropdown вне компонента
import { createPortal } from 'react-dom'

return createPortal(
    <div className={cls.menu}>...</div>,
    document.body
)
```
**Минусы**: Сложнее, требует дополнительной логики позиционирования

### 2. CSS isolation
```scss
.container {
    isolation: isolate; // Создать новый контекст
}
```
**Минусы**: Может сломать другие элементы

### 3. Фиксированное позиционирование
```scss
.menu {
    position: fixed; // Вместо absolute
}
```
**Минусы**: Нужно вычислять абсолютные координаты

---

## ✨ Результат

✅ Dropdown теперь всегда отображается поверх форм  
✅ Не требуется изменений в структуре компонента  
✅ Простое и эффективное решение  
✅ Не влияет на другие компоненты  

---

**Дата**: 04.02.2026  
**Версия**: 1.2.1 (hotfix)  
**Статус**: ✅ Исправлено
