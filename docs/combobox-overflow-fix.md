# Исправление: Dropdown обрезается родительским контейнером

## 🐛 Проблема

Dropdown в `ClientSelectV3` открывался только в пределах родительского `HStack` (`searchRow`):
- Появлялся скролл в HStack
- Dropdown перекрывал Input
- Список не выходил за пределы контейнера

**Причина**: `overflow-x: hidden` на родительских контейнерах обрезал dropdown с `position: absolute`.

---

## ✅ Решение

Удалён `overflow-x: hidden` из родительских контейнеров, которые содержат Combobox.

### Изменённые файлы

**1. AssistantsListHeader.module.scss**

```scss
.AssistantsListHeader {
    width: 100%;
    max-width: 100%;
    padding: var(--space-4) 0;
    // overflow-x: hidden; - ❌ Удалено
}

.searchRow {
    width: 100%;
    max-width: 100%;
    align-items: center;
    // overflow-x: hidden; - ❌ Удалено
}
```

---

## 🔍 Как работает overflow и position

### Проблема

```
┌─ HStack (overflow-x: hidden) ─┐
│  ┌─ Combobox ────────────┐    │
│  │  Control              │    │
│  │  ┌─ Dropdown ─────┐   │    │ ← Dropdown обрезается!
│  │  │ Item 1         │   │    │
│  │  │ Item 2         │   │    │
│  │  └────────────────┘   │    │
│  └───────────────────────┘    │
└────────────────────────────────┘
```

### Решение

```
┌─ HStack (no overflow) ────────┐
│  ┌─ Combobox ────────────┐    │
│  │  Control              │    │
│  └───────────────────────┘    │
│     ┌─ Dropdown ──────────┐   │
│     │ Item 1              │   │ ← Dropdown свободен!
│     │ Item 2              │   │
│     │ Item 3              │   │
│     └─────────────────────┘   │
└────────────────────────────────┘
```

---

## 📚 Правила работы с overflow

### ❌ НЕ использовать overflow: hidden когда:
1. Внутри есть элементы с `position: absolute` (dropdown, tooltip)
2. Внутри есть элементы с `position: fixed`
3. Нужны выпадающие меню

### ✅ Можно использовать overflow: hidden когда:
1. Нужно обрезать контент (например, карточки с текстом)
2. Нет dropdown/tooltip/popup элементов
3. Используется для создания Stacking Context

### 💡 Альтернативы

Если нужно предотвратить горизонтальную прокрутку, но сохранить dropdown:

```scss
// ❌ Плохо - обрезает dropdown
overflow-x: hidden;

// ✅ Хорошо - только для мобильных
@include mobile {
    overflow-x: clip; // Или используйте max-width: 100vw
}

// ✅ Хорошо - на правильном уровне
.pageWrapper {
    overflow-x: hidden; // На уровне страницы
}

.header {
    // Без overflow - dropdown работает
}
```

---

## 🎯 Рекомендации для других компонентов

### При использовании Combobox, Select, Dropdown:

```scss
// ✅ Правильно
.container {
    width: 100%;
    max-width: 100%;
    // НЕ ДОБАВЛЯТЬ overflow-x: hidden
}

// ❌ Неправильно
.container {
    overflow-x: hidden; // Сломает dropdown!
}
```

### Если нужен overflow:

```scss
// Вариант 1: Использовать на более высоком уровне
.page {
    overflow-x: hidden;
}

// Вариант 2: Только для определённых элементов
.textContent {
    overflow: hidden;
    text-overflow: ellipsis;
}

// Вариант 3: Portal для dropdown
// Рендерить dropdown вне контейнера через React Portal
```

---

## 🧪 Тестирование

### Чек-лист после исправления:

- [x] Dropdown открывается полностью
- [x] Нет горизонтальной прокрутки в HStack
- [x] Dropdown не перекрывает Input
- [x] Dropdown имеет правильный z-index (1000)
- [x] Работает на десктопе
- [x] Работает на мобильных
- [x] Не появилась горизонтальная прокрутка страницы

---

## 📊 До и После

### До (с overflow-x: hidden):
```
HStack [overflow-x: hidden]
  ├─ Input ✅
  └─ Combobox
      └─ Dropdown ❌ (обрезан, скролл в HStack)
```

### После (без overflow):
```
HStack
  ├─ Input ✅
  └─ Combobox
      └─ Dropdown ✅ (отображается полностью)
```

---

## 💡 Связанные исправления

Эта проблема связана с:
1. **Z-index fix** - также был увеличен z-index до 1000
2. **Mobile adaptation** - overflow был добавлен для предотвращения прокрутки

**Важно**: При добавлении `overflow-x: hidden` для мобильной адаптации, убедитесь, что внутри нет dropdown элементов!

---

**Дата**: 04.02.2026  
**Версия**: 1.2.2 (hotfix)  
**Статус**: ✅ Исправлено
