# Рефакторинг: Замена ClientSelect на версию V3

## 🎯 Цель

Полная замена устаревшего компонента `ClientSelect` (на базе MUI) на новый `ClientSelect` (на базе redesign-v3 Combobox) во всём приложении.

---

## 🛠 Изменения

### 1. Компонент

*   Старый компонент `src/entities/User/ui/ClientSelect` удалён.
*   Новый компонент `src/entities/User/ui/ClientSelectV3` переименован в `ClientSelect` и перемещён на место старого.
*   Теперь `ClientSelect` использует **redesign-v3 Combobox**.

### 2. Интерфейс

Новый компонент имеет упрощённый API, работающий с ID вместо объектов:

**Было:**
```tsx
<ClientSelect
    value={userObject} // { id: string, name: string }
    onChangeClient={(event, newValue) => { ... }}
/>
```

**Стало:**
```tsx
<ClientSelect
    clientId={userId} // string
    onChangeClient={(id) => { ... }} // (id: string) => void
/>
```

### 3. Обновленные компоненты

Следующие компоненты были обновлены для поддержки нового API:

*   **AssistantsListHeader**
*   **AssistantForm / MainInfoCard**
*   **PbxServersListHeader**
*   **PbxServerCard / GeneralSection**
*   **ToolsListHeader**
*   **ToolForm / GeneralToolCard**
*   **PublishWidgetsListHeader**
*   **PeriodExtendedFilter**

### 4. Хуки фильтрации

Обновлены хуки для приема `string` id вместо события и объекта:
*   `useAssistantFilters`
*   `usePbxServersFilters`
*   `useToolsFilters`
*   `usePublishWidgetsFilters`

---

## 🔍 Особенности миграции

1.  **Удаление иконок**: Новый `ClientSelect` (Combobox) поддерживает иконки внутри через `addonLeft`. Внешние обёртки с иконками были удалены из хедеров списков.
2.  **Обработка данных**: В формах, где Redux ожидает объект `user { id, name }`, мы теперь создаем объект с пустым именем `{ id: newId, name: '' }`, так как новый селект не возвращает имя (оно инкапсулировано). Это безопасно, так как Combobox сам подтягивает имя по ID для отображения, а бэкенду обычно важен только ID.

---

**Дата**: 04.02.2026
**Статус**: ✅ Завершено
