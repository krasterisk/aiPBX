---
phase: 09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso
plan: 05
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/features/Auth/lib/hooks/useSignupData.ts
  - src/features/Auth/lib/hooks/useSignupData.test.ts
  - src/app/App.tsx
  - src/pages/Playground/ui/Playground/Playground.tsx
  - src/pages/PaymentPage/ui/PaymentPage.tsx
  - src/pages/PaymentPage/ui/PaymentPage.test.tsx
autonomous: false
requirements: [D-06, D-07]
must_haves:
  truths:
    - "On each of the 3 signup success handlers (google/telegram/email-activation), trackEvent('signup_complete',{method}) and fireAdsConversion(__ADS_SIGNUP_LABEL__) fire"
    - "A route-change effect fires gtag page_view (manual) on every navigation (send_page_view:false owns first view)"
    - "first_call funnel event is emitted (reconciled with existing playground_call_success)"
    - "After the blocking founder decision, payment_success fires on a named non-billing-core success surface without editing billing/payment API logic"
  artifacts:
    - path: "src/features/Auth/lib/hooks/useSignupData.ts"
      provides: "signup_complete + Ads conversion at 3 success sites"
      contains: "signup_complete"
    - path: "src/features/Auth/lib/hooks/useSignupData.test.ts"
      provides: "funnel unit test (mock trackEvent/fireAdsConversion)"
      contains: "signup_complete"
    - path: "src/app/App.tsx"
      provides: "SPA route-change page_view"
      contains: "page_view"
  key_links:
    - from: "src/features/Auth/lib/hooks/useSignupData.ts"
      to: "src/shared/config/analytics/initAnalytics.ts"
      via: "import { trackEvent, fireAdsConversion }"
      pattern: "fireAdsConversion"
---

<objective>
Wire the full GA4 funnel (D-06) + Google Ads conversion (D-07) into the app, building on
`fireAdsConversion`/`trackEvent` from 09-03. Fire `signup_complete` + Ads conversion at the
three signup success sites, add SPA route-change `page_view` (paired with GA4
`send_page_view:false`), reconcile `first_call`, and, after the blocking founder decision,
fire `payment_success` from the approved NON-invasive frontend success surface — WITHOUT touching backend
`billing/`/`ari/`/`accounting/` or frontend payment API/checkout logic (per `.cursor/rules`
+ RESEARCH A5). Add the funnel unit test (Wave-0 gap).

Purpose: this is the measurable conversion layer Google Ads imports; the signup conversion
label `-B6_CK72wtMcEIyDxKA-` must fire on real signup completion.
Output: instrumented useSignupData + funnel test + page_view + payment_success.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/intel/GTM-CONTENT-PLAN.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-PATTERNS.md
@src/features/Auth/lib/hooks/useSignupData.ts
@src/app/App.tsx
@src/pages/PaymentPage/ui/PaymentPage.tsx
</context>

## Artifacts this phase produces (this plan)

- `signup_complete` GA4 event + Ads conversion at 3 useSignupData `.then()` success sites (method: google/telegram/email)
- SPA route-change `page_view` effect in App.tsx (uses existing `useLocation`)
- `first_call` funnel event reconciliation (non-breaking; keep playground_call_success)
- `payment_success` event fired non-invasively on the founder-approved frontend payment success surface
- `useSignupData.test.ts` (funnel unit test)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fire signup_complete + Ads conversion at 3 signup success sites</name>
  <files>src/features/Auth/lib/hooks/useSignupData.ts</files>
  <read_first>
    - src/features/Auth/lib/hooks/useSignupData.ts (Google 58-70, Telegram 74-86, email-activation onSignupActivateClick 124-145; note onSignupClick 90-117 is the code-SENT step — do NOT fire there)
    - src/shared/config/analytics/initAnalytics.ts (trackEvent + fireAdsConversion from 09-03)
    - 09-RESEARCH.md § "Analytics Wiring Map" ; 09-PATTERNS.md § "useSignupData.ts"
  </read_first>
  <behavior>
    - Google signup success → trackEvent('signup_complete',{method:'google'}) AND fireAdsConversion(__ADS_SIGNUP_LABEL__)
    - Telegram signup success → same with method:'telegram'
    - Email activation success (onSignupActivateClick .then) → same with method:'email'
    - No conversion fires on onSignupClick success (email-code-sent step) or in any .catch
  </behavior>
  <action>
    Add `import { trackEvent, fireAdsConversion } from '@/shared/config/analytics/initAnalytics'`.
    Inside the `.then()` success callback of `handleGoogleSignupSuccess`, `handleTelegramSignupSuccess`,
    and `onSignupActivateClick`, add two calls: `trackEvent('signup_complete', { method })` (method =
    'google' | 'telegram' | 'email') and `fireAdsConversion(__ADS_SIGNUP_LABEL__)`. Read the Ads label via
    the `typeof __ADS_SIGNUP_LABEL__ !== 'undefined' ? __ADS_SIGNUP_LABEL__ : ''` guard (or pass through
    directly since fireAdsConversion no-ops on empty). Do NOT add tracking to `onSignupClick` (that only
    sends the email code) or to any `.catch`. Send no PII in params (method string only).
  </action>
  <verify>
    <automated>npm run lint:ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "signup_complete" src/features/Auth/lib/hooks/useSignupData.ts` returns 3
    - `grep -c "fireAdsConversion" src/features/Auth/lib/hooks/useSignupData.ts` returns 3 (plus 1 import)
    - No tracking added in onSignupClick or any .catch
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>Signup completion fires GA4 event + Ads conversion at all 3 success sites.</done>
</task>

<task type="auto">
  <name>Task 2: useSignupData funnel unit test</name>
  <files>src/features/Auth/lib/hooks/useSignupData.test.ts</files>
  <read_first>
    - src/features/Onboarding/lib/onboardingAnalytics.test.ts (mock idiom)
    - src/features/Auth/lib/hooks/useSignupData.ts (handlers to exercise)
    - 09-VALIDATION.md § "Per-Task Verification Map" (D-06/D-07 row)
  </read_first>
  <action>
    Create useSignupData.test.ts. Mock `@/shared/config/analytics/initAnalytics` exporting jest.fn()
    `trackEvent` and `fireAdsConversion`. Mock the RTK Query signup hooks so the success path resolves
    (`.unwrap().then()` fires) — mock `@/entities/User` hooks to return a mutation trigger whose call
    returns `{ unwrap: () => Promise.resolve({...token}) }`, and mock router/dispatch as needed (follow
    the repo's hook-test convention; use renderHook from @testing-library/react if available, else test the
    exported handlers directly). Assert that on a resolved google/telegram/email success,
    `trackEvent` was called with `('signup_complete', { method })` and `fireAdsConversion` was called.
    If full hook rendering is impractical, at minimum assert the analytics module is invoked with the
    correct event name + Ads label via a focused test of each success callback.
  </action>
  <verify>
    <automated>npm run test:unit -- useSignupData</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- useSignupData` passes
    - Test asserts signup_complete event name + fireAdsConversion invocation on success (D-06/D-07)
  </acceptance_criteria>
  <done>Funnel firing proven by green unit test.</done>
</task>

<task type="auto">
  <name>Task 3: SPA page_view route-change + first_call instrumentation</name>
  <files>src/app/App.tsx, src/pages/Playground/ui/Playground/Playground.tsx</files>
  <read_first>
    - src/app/App.tsx (existing useLocation lines 13,25)
    - 09-RESEARCH.md § "SPA page_view on route change" + § "Analytics Wiring Map" (first_call/payment_success rows + A5)
    - .cursor/rules/aipbx-core.mdc (do NOT touch billing/ari/accounting)
  </read_first>
  <action>
    In App.tsx add a `useEffect` keyed on `pathname` that, when `__GA4_MEASUREMENT_ID__` is set and
    `window.gtag` exists, calls `window.gtag('event','page_view',{ page_path: pathname, page_location:
    __SITE_URL__ + pathname, page_title: document.title })`. This pairs with GA4 `send_page_view:false`
    from 09-03 (Pitfall 5 — no double count). For `first_call`: locate the existing
    `playground_call_success` emission in `src/pages/Playground/ui/Playground/Playground.tsx` and,
    non-breaking, ALSO emit `trackEvent('first_call')` there; keep `playground_call_success` for
    continuity. Do not touch billing/ari/accounting while doing this.
  </action>
  <verify>
    <automated>npm run lint:ts</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx fires a manual gtag page_view on pathname change
    - Playground.tsx emits `first_call` alongside the existing `playground_call_success`
    - No edits to backend billing/ari/accounting or to entities/Payment/api or CheckoutByRobokassa
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>Route-change page_view and non-breaking first_call instrumentation are wired without touching billing core.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4: Founder decision for payment_success success surface</name>
  <files>src/pages/PaymentPage/ui/PaymentPage.tsx</files>
  <read_first>
    - .planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-CONTEXT.md (D-06 funnel scope; billing/ari/accounting out of scope)
    - .planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md § "Analytics Wiring Map" + § "Open Questions" (A5 — non-invasive payment_success placement)
    - src/pages/PaymentPage/ui/PaymentPage.tsx (candidate frontend payment route; inspect for an existing success/return signal)
  </read_first>
  <decision>Confirm the non-billing frontend success surface for `payment_success` before implementation.</decision>
  <context>
    `src/pages/PaymentPage/ui/PaymentPage.tsx` is the concrete frontend payment route, but the current
    component contains no explicit top-up/Robokassa success branch. The event cannot be honestly wired
    from a generic page mount because that would count page visits as payments. Billing, ARI, accounting,
    payment API, and checkout core remain out of scope.
  </context>
  <options>
    <option id="payment-page-success-branch">
      <name>Use PaymentPage success branch</name>
      <pros>Keeps the event on a named non-billing route component and avoids payment-core edits.</pros>
      <cons>Requires an existing or explicitly exposed success signal in PaymentPage.</cons>
    </option>
    <option id="alternate-return-route">
      <name>Name an alternate non-billing payment return component</name>
      <pros>Uses the actual post-payment return signal if it is outside PaymentPage.</pros>
      <cons>Founder must identify the concrete route file before execution.</cons>
    </option>
  </options>
  <resume-signal>Choose payment-page-success-branch or alternate-return-route and name the target component if different.</resume-signal>
  <action>Before any `payment_success` implementation, record the founder's selected option and the exact
  frontend component path. The selected path must expose a real payment-success signal; a page mount alone
  is invalid. Keep `billing/`, `ari/`, `accounting/`, payment API, and checkout core untouched.</action>
  <verify>
    <automated>MISSING — blocking founder decision must record the approved non-billing target component before implementation.</automated>
  </verify>
  <acceptance_criteria>
    - Founder selects one option and records the exact target component path
    - No payment_success implementation proceeds without that concrete success signal
  </acceptance_criteria>
  <done>Payment success placement is explicitly approved or blocked with a named non-billing target.</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 5: Wire payment_success in the approved PaymentPage return/success component</name>
  <files>src/pages/PaymentPage/ui/PaymentPage.tsx</files>
  <read_first>
    - src/pages/PaymentPage/ui/PaymentPage.tsx (founder-approved payment return/success component)
    - src/shared/config/analytics/initAnalytics.ts (trackEvent export)
    - 09-RESEARCH.md § "Analytics Wiring Map" and § "Open Questions" (A5; keep billing/ARI/accounting/payment API/checkout core untouched)
  </read_first>
  <action>
    After Task 4 records the founder-approved concrete success signal, wire
    `trackEvent('payment_success')` into that confirmed success branch in
    `src/pages/PaymentPage/ui/PaymentPage.tsx` (or the explicitly named alternate
    non-billing frontend payment return component selected at the checkpoint). Do not
    fire on generic page mount, before payment success, or from a catch/error path.
    Add or update the colocated Jest test to mock `trackEvent`/gtag and assert the
    event is emitted from the confirmed success branch, with a negative assertion
    for mount/failure paths.
    Keep the implementation read-only with respect to payment state and do not edit
    `billing/`, `ari/`, `accounting/`, `entities/Payment/api`, or
    `CheckoutByRobokassa`. If the checkpoint selects an alternate component, update
    this task's file target to that exact confirmed path before implementation.
  </action>
  <verify>
    <automated>npm run test:unit -- PaymentPage</automated>
  </verify>
  <acceptance_criteria>
    - The founder-approved non-billing payment return/success component emits exactly one `trackEvent('payment_success')` for a confirmed successful payment.
    - The Jest test mocks `trackEvent`/gtag and fails if `payment_success` is not emitted from the success branch.
    - No event fires on generic page mount or payment failure.
    - No billing, ARI, accounting, payment API, or checkout-core files are modified.
    - `npm run test:unit -- PaymentPage` passes.
  </acceptance_criteria>
  <done>payment_success is proven by a fail-closed unit assertion in the founder-approved non-billing success component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client event params → GA4/Ads | conversion + funnel payloads |
| analytics wiring ↔ billing/payment | must remain read-only to payment logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Information disclosure | signup/payment event params | mitigate | Only non-PII params sent (method string, no email/token/amount identifiers). |
| T-09-11 | Tampering | payment logic scope | mitigate | payment_success fired from a read-only success surface; hard constraint forbids editing billing/ari/accounting/payment-api. |
| T-09-12 | Repudiation | conversion double/missing fire | mitigate | Fire only in `.then()` success (not code-sent step, not .catch); unit test asserts exactly-once per success. |
</threat_model>

<verification>
- `npm run test:unit -- useSignupData` green.
- `npm run lint:ts` green.
- A Jest funnel assertion in `npm run test:unit -- useSignupData` proves `signup_complete` and Ads conversion calls fail-closed.
- No diff under billing/ari/accounting/entities/Payment/api/CheckoutByRobokassa.
</verification>

<success_criteria>
Full GA4 funnel (signup_complete, page_view, first_call, payment_success) + Ads signup
conversion fire correctly after the explicit payment-surface decision; billing core untouched (D-06/D-07).
</success_criteria>

<output>
Create `.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-05-SUMMARY.md` when done
</output>
