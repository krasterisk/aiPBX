---
phase: 09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - public/sitemap.xml
  - public/robots.txt
  - public/assets/og-default.png
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "sitemap.xml and robots.txt reference https://aipbx.net and contain no aipbx.ru URLs"
    - "sitemap lists the 4 public routes (/, /pricing, /speech-analytics, /voice-assistants) with lastmod"
    - "public/assets/og-default.png exists at 1200x630"
  artifacts:
    - path: "public/sitemap.xml"
      provides: "aipbx.net sitemap with lastmod"
      contains: "aipbx.net"
    - path: "public/robots.txt"
      provides: "aipbx.net Sitemap line, public Allow / app Disallow"
      contains: "aipbx.net"
    - path: "public/assets/og-default.png"
      provides: "1200x630 default OG image"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap: https://aipbx.net/sitemap.xml"
      pattern: "aipbx.net/sitemap.xml"
---

<objective>
Switch the static SEO assets to the `.net` domain (D-03) and generate the missing default
OG image (D-05). Single-build constraint: one sitemap/robots per build; RU variants deferred
to Phase 4. This unblocks the prerender verify gate (09-07) and fixes broken OG previews.

Purpose: crawlers/GSC need a correct `.net` sitemap+robots; social scrapers need a real
1200x630 OG image (currently referenced but absent).
Output: updated sitemap.xml + robots.txt + new og-default.png.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md
@scripts/aipbx_seo.patch
@public/sitemap.xml
@public/robots.txt
</context>

## Artifacts this phase produces (this plan)

- `public/sitemap.xml` → `https://aipbx.net` URLs + `<lastmod>` (per patch)
- `public/robots.txt` → `Sitemap: https://aipbx.net/sitemap.xml`
- `public/assets/og-default.png` (1200x630 PNG) — the file referenced by usePageMeta `/assets/og-default.png`

<tasks>

<task type="auto">
  <name>Task 1: Switch sitemap.xml and robots.txt to aipbx.net</name>
  <files>public/sitemap.xml, public/robots.txt</files>
  <read_first>
    - public/sitemap.xml (current aipbx.ru URLs)
    - public/robots.txt (current Sitemap line + Disallow rules)
    - scripts/aipbx_seo.patch (sitemap + robots hunks — apply as baseline)
    - 09-RESEARCH.md § "sitemap.xml / robots.txt strategy"
  </read_first>
  <action>
    In public/sitemap.xml replace every `https://aipbx.ru` with `https://aipbx.net` and add a
    `<lastmod>` (current date, e.g. 2026-07-21) to each `<url>` per the patch. Ensure the public routes
    `/`, `/pricing`, `/speech-analytics`, `/voice-assistants` are present. In public/robots.txt change the
    `Sitemap:` line to `https://aipbx.net/sitemap.xml`; keep app-route `Disallow` rules
    (`/playground`, `/payment`, `/admin`, etc.) and public `Allow`. Do NOT add RU variants (Phase 4).
  </action>
  <verify>
    <automated>grep -q "aipbx.net" public/sitemap.xml && grep -q "aipbx.net/sitemap.xml" public/robots.txt && ! grep -q "aipbx.ru" public/sitemap.xml && ! grep -q "aipbx.ru" public/robots.txt && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - No `aipbx.ru` remains in sitemap.xml or robots.txt
    - sitemap.xml contains the 4 public routes with lastmod
    - robots.txt Sitemap line points to aipbx.net
  </acceptance_criteria>
  <done>Static SEO assets serve the .net domain.</done>
</task>

<task type="auto">
  <name>Task 2: Generate og-default.png (1200x630)</name>
  <files>public/assets/og-default.png</files>
  <read_first>
    - src/shared/lib/seo/usePageMeta.ts (DEFAULT_OG_IMAGE = '/assets/og-default.png')
    - 09-RESEARCH.md § "Don't Hand-Roll" (OG image row) + § "Pitfall 6"
  </read_first>
  <action>
    Create a 1200x630 PNG at public/assets/og-default.png — an AI PBX branded card (logo/wordmark +
    tagline "AI Voice Assistant & Speech Analytics"). Generate with an image tool (e.g. sharp/Canva/
    Figma export or an image-generation step); the file must be a valid PNG, dimensions exactly 1200x630,
    under 8MB. It is served as an absolute URL `${SITE_URL}/assets/og-default.png` by usePageMeta.
  </action>
  <verify>
    <automated>test -f public/assets/og-default.png && node -e "const fs=require('fs');const b=fs.readFileSync('public/assets/og-default.png');const w=b.readUInt32BE(16),h=b.readUInt32BE(20);if(w!==1200||h!==630){console.error('BAD DIMS',w,h);process.exit(1)}console.log('OG 1200x630 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - public/assets/og-default.png exists and is a valid PNG
    - Dimensions are exactly 1200x630
  </acceptance_criteria>
  <done>Default OG image exists at correct dimensions; previews no longer broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static files → crawlers/scrapers | flat files served from build/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Spoofing | sitemap/robots domain | mitigate | Hardcoded to https://aipbx.net; grep gate asserts no aipbx.ru leakage. |
| T-09-14 | Info disclosure | robots Disallow scope | accept | App routes (/payment, /admin, /playground) remain Disallowed; no sensitive paths newly exposed. |
</threat_model>

<verification>
- Task 1 grep gate prints PASS.
- Task 2 dimension check prints OG 1200x630 OK.
</verification>

<success_criteria>
sitemap/robots serve aipbx.net (no .ru), and a valid 1200x630 og-default.png exists (D-03/D-05).
</success_criteria>

<output>
Create `.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-06-SUMMARY.md` when done
</output>
