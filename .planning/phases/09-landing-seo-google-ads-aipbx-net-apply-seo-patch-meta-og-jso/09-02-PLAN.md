---
phase: 09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/shared/lib/seo/usePageMeta.ts
  - src/shared/lib/seo/useSeoRenderReady.ts
  - src/shared/lib/seo/usePageMeta.test.ts
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "setPageMeta writes title, description, og:title/description/type/image, canonical, og:url, hreflang(en/ru/x-default), and JSON-LD into document.head"
    - "canonical/og:url/hreflang are built from __SITE_URL__ (https://aipbx.net), NOT window.location.origin"
    - "hreflang en → aipbx.net, ru → aipbx.ru, x-default → aipbx.net"
    - "useSeoRenderReady dispatches document event 'seo-render-ready' once i18n ready"
  artifacts:
    - path: "src/shared/lib/seo/usePageMeta.ts"
      provides: "i18n-driven meta helper with JSON-LD + hreflang + build-time SITE_URL canonical"
      contains: "__SITE_URL__"
    - path: "src/shared/lib/seo/useSeoRenderReady.ts"
      provides: "hook dispatching 'seo-render-ready' after i18n ready"
      contains: "seo-render-ready"
    - path: "src/shared/lib/seo/usePageMeta.test.ts"
      provides: "jsdom head assertions for D-01/D-02"
      contains: "canonical"
  key_links:
    - from: "src/shared/lib/seo/usePageMeta.ts"
      to: "__SITE_URL__"
      via: "module const SITE_URL = typeof __SITE_URL__ !== 'undefined' ? __SITE_URL__ : 'https://aipbx.net'"
      pattern: "__SITE_URL__"
---

<objective>
Extend `usePageMeta` (do NOT replace with react-helmet) so it is i18n-value-driven and
emits the full crawlable head: description, OG tags, canonical, og:url, hreflang
alternates (en/ru/x-default), and per-page JSON-LD — all built from the build-time
`__SITE_URL__` constant instead of `window.location.origin` (Pitfall 3). Add a shared
`useSeoRenderReady` hook that dispatches the `seo-render-ready` document event the
prerenderer waits on. Add the jsdom unit test (Wave-0 gap) covering D-01/D-02.

Purpose: bots must receive correct absolute canonical/OG/hreflang/JSON-LD in the
prerendered HTML; runtime `window.location` is `localhost` under prerender.
Output: extended helper + render-ready hook + unit test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-PATTERNS.md
@src/shared/lib/seo/usePageMeta.ts
</context>

## Artifacts this phase produces (this plan)

- Extended `setPageMeta`/`usePageMeta` with `ogImage`, `jsonLd` options + hreflang + JSON-LD upsert
- New `upsertHreflang` and `upsertJsonLd` head-upsert helpers (same idiom as `upsertMeta`/`upsertCanonical`)
- Module consts `SITE_URL` (from `__SITE_URL__`, default `https://aipbx.net`) and `RU_SITE_URL` (`https://aipbx.ru`)
- `useSeoRenderReady()` hook → dispatches `seo-render-ready` event (consumed by prerender plan 09-07)
- `usePageMeta.test.ts` (jsdom head assertions)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend usePageMeta — hreflang, JSON-LD, build-time SITE_URL canonical</name>
  <files>src/shared/lib/seo/usePageMeta.ts</files>
  <read_first>
    - src/shared/lib/seo/usePageMeta.ts (full file — upsert idiom lines 13-38; setPageMeta lines 40-55; consts 10-11; hook dep array 57-61)
    - 09-PATTERNS.md § "usePageMeta.ts" (exact upsert idiom + origin-to-replace)
    - 09-RESEARCH.md § "Code Examples → Extended usePageMeta" and § "Pattern 2"
  </read_first>
  <behavior>
    - setPageMeta with `path:'/speech-analytics'` sets `<link rel="canonical">` href to `https://aipbx.net/speech-analytics` (uses SITE_URL, not localhost)
    - hreflang en → `https://aipbx.net/<path>`, ru → `https://aipbx.ru/<path>`, x-default → `https://aipbx.net/<path>`
    - og:image resolves to absolute `${SITE_URL}${ogImage||'/assets/og-default.png'}`
    - passing `jsonLd` injects one `<script type="application/ld+json" id="page-jsonld">` with that JSON; calling again updates (does not duplicate)
    - calling setPageMeta twice is idempotent (no duplicate meta/link/script nodes)
  </behavior>
  <action>
    Add module consts next to SITE_NAME/DEFAULT_OG_IMAGE:
    `const SITE_URL = typeof __SITE_URL__ !== 'undefined' ? __SITE_URL__ : 'https://aipbx.net'`
    and `const RU_SITE_URL = 'https://aipbx.ru'`. Add `ogImage?` (already present) and
    `jsonLd?: Record<string, unknown>` to `PageMetaOptions`. Add two helpers mirroring the
    existing create-or-update idiom: `upsertHreflang(lng, href)` upserting
    `<link rel="alternate" hreflang="{lng}">` keyed by `hreflang` attr, and
    `upsertJsonLd(data)` upserting `<script type="application/ld+json" id="page-jsonld">`
    (set `.textContent = JSON.stringify(data)`). In setPageMeta: make og:image absolute
    `${SITE_URL}${ogImage || DEFAULT_OG_IMAGE}`; in the `if (path)` block REPLACE
    `window.location.origin` with `SITE_URL` for canonical + og:url, then call
    `upsertHreflang('en', \`${SITE_URL}${path}\`)`, `upsertHreflang('ru', \`${RU_SITE_URL}${path}\`)`,
    `upsertHreflang('x-default', \`${SITE_URL}${path}\`)`. If `jsonLd` provided call `upsertJsonLd`.
    Extend the `usePageMeta` useEffect dep array to include `options.jsonLd`. JSON-LD is
    developer-authored (no user input) — safe to stringify directly.
  </action>
  <verify>
    <automated>npm run lint:ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "window.location.origin" src/shared/lib/seo/usePageMeta.ts` returns 0
    - File contains `__SITE_URL__`, `upsertHreflang`, `upsertJsonLd`, `hreflang`, `application/ld+json`
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>Helper emits i18n-driven absolute canonical/hreflang/OG/JSON-LD from SITE_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Add useSeoRenderReady hook + usePageMeta jsdom test</name>
  <files>src/shared/lib/seo/useSeoRenderReady.ts, src/shared/lib/seo/usePageMeta.test.ts</files>
  <read_first>
    - src/features/Onboarding/lib/onboardingAnalytics.test.ts (test skeleton — clearAllMocks, describe/it)
    - 09-RESEARCH.md § "Pattern 1" (renderAfterDocumentEvent) and § "Validation Architecture"
    - 09-PATTERNS.md § "initAnalytics.test.ts & usePageMeta.test.ts" (test-specific guidance)
  </read_first>
  <action>
    Create useSeoRenderReady.ts exporting `useSeoRenderReady(ready: boolean)`: a hook with
    `useEffect(() => { if (ready) document.dispatchEvent(new Event('seo-render-ready')) }, [ready])`.
    Event name string MUST be exactly `seo-render-ready` (consumed by prerender plugin config).
    Create usePageMeta.test.ts (jsdom is default env): call `setPageMeta({ title, description,
    path:'/speech-analytics', jsonLd:{...} })` and assert via `document.head.querySelector`:
    `meta[name="description"]` content, `link[rel="canonical"]` href equals
    `https://aipbx.net/speech-analytics` (assert it does NOT contain `localhost`),
    `link[rel="alternate"][hreflang="en"]` → aipbx.net, `[hreflang="ru"]` → aipbx.ru,
    `[hreflang="x-default"]` → aipbx.net, `script[type="application/ld+json"]` textContent parses
    to the passed object. Add an idempotency test: call twice, assert exactly one canonical and
    one `#page-jsonld`. `__SITE_URL__` is provided by jest globals (09-01); no per-test define needed.
  </action>
  <verify>
    <automated>npm run test:unit -- usePageMeta</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- usePageMeta` passes
    - Test asserts canonical uses `https://aipbx.net` and NOT `localhost`/`window.origin`
    - Test asserts hreflang en/ru/x-default and JSON-LD presence + idempotency
    - useSeoRenderReady.ts contains the literal `seo-render-ready`
  </acceptance_criteria>
  <done>Render-ready hook exists; D-01/D-02 covered by green jsdom test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer content → document.head | JSON-LD/meta strings authored in code + i18n files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering/XSS | upsertJsonLd | accept | JSON-LD data is developer/i18n-authored (not user input) and injected via `.textContent`/`JSON.stringify` (no HTML parsing). ASVS V5: if any dynamic/user value ever enters jsonLd, it is already JSON-encoded. |
| T-09-04 | Spoofing | canonical/hreflang host | mitigate | Host fixed to `__SITE_URL__` build constant; test asserts no `localhost`. |
</threat_model>

<verification>
- `npm run test:unit -- usePageMeta` green.
- `npm run lint:ts` green.
- `grep -c seo-render-ready src/shared/lib/seo/useSeoRenderReady.ts` ≥1.
</verification>

<success_criteria>
usePageMeta emits full crawlable head from i18n values + `__SITE_URL__`; render-ready
hook dispatches `seo-render-ready`; jsdom test proves D-01/D-02.
</success_criteria>

<output>
Create `.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-02-SUMMARY.md` when done
</output>
