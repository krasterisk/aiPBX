---
phase: 09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/shared/config/analytics/initAnalytics.ts
  - src/shared/config/analytics/initAnalytics.test.ts
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "initAnalytics configures Google Ads (gtag config __GOOGLE_ADS_ID__) in addition to GA4 when the Ads ID env is set"
    - "GA4 config uses send_page_view:false so the SPA can own manual page_view (Pitfall 5)"
    - "fireAdsConversion(label, params) calls gtag('event','conversion',{send_to:`${adsId}/${label}`}) only when Ads ID + window.gtag exist"
    - "All new analytics code is a no-op when the corresponding build-env ID is unset"
  artifacts:
    - path: "src/shared/config/analytics/initAnalytics.ts"
      provides: "Ads config + fireAdsConversion export; send_page_view:false"
      exports: ["initAnalytics", "trackEvent", "fireAdsConversion"]
      contains: "fireAdsConversion"
    - path: "src/shared/config/analytics/initAnalytics.test.ts"
      provides: "mock-gtag unit test for D-06/D-07"
      contains: "conversion"
  key_links:
    - from: "src/shared/config/analytics/initAnalytics.ts"
      to: "window.gtag"
      via: "guarded gtag('config', adsId) + gtag('event','conversion',...)"
      pattern: "__GOOGLE_ADS_ID__"
---

<objective>
Extend the existing `initAnalytics` module (do NOT hardcode gtag in `index.html`) to
also configure Google Ads and expose `fireAdsConversion` for the signup/payment funnel
(D-06/D-07). Set GA4 `send_page_view:false` so the SPA owns manual `page_view`
(prevents double counting, Pitfall 5). Add the mock-gtag unit test (Wave-0 gap).

Purpose: analytics IDs live in build env; a single gtag.js load configures both G- and
AW- IDs; Ads conversion is Google-only. This is the reusable primitive consumed by the
funnel wiring plan (09-05).
Output: extended initAnalytics + unit test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-PATTERNS.md
@src/shared/config/analytics/initAnalytics.ts
@src/features/Onboarding/lib/onboardingAnalytics.test.ts
</context>

## Artifacts this phase produces (this plan)

- Second `gtag('config', __GOOGLE_ADS_ID__)` after GA4 config (guarded on `window.gtag`)
- GA4 config option `{ send_page_view: false }` (SPA owns page_view via 09-05)
- New export `fireAdsConversion(label: string, params?: Record<string, unknown>)`
- `initAnalytics.test.ts` (mock `window.gtag`, per-test build-constant overrides)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Ads config + fireAdsConversion to initAnalytics</name>
  <files>src/shared/config/analytics/initAnalytics.ts</files>
  <read_first>
    - src/shared/config/analytics/initAnalytics.ts (full — env-guard idiom lines 20-21; GA4 config lines 36-45; trackEvent guard lines 47-58)
    - 09-RESEARCH.md § "Code Examples → Extend initAnalytics" and § "Pitfall 5"
    - 09-PATTERNS.md § "initAnalytics.ts"
  </read_first>
  <behavior>
    - When `__GA4_MEASUREMENT_ID__` set: gtag('config', ga4Id, { send_page_view: false }) is called
    - When `__GOOGLE_ADS_ID__` set (and window.gtag exists): an additional gtag('config', adsId) is called
    - fireAdsConversion('-B6_CK72wtMcEIyDxKA-') → gtag('event','conversion',{ send_to: 'AW-16711221644/-B6_CK72wtMcEIyDxKA-' })
    - fireAdsConversion is a no-op when __GOOGLE_ADS_ID__ is '' or window.gtag is undefined
    - fireAdsConversion merges extra params into the conversion payload
  </behavior>
  <action>
    Read `__GOOGLE_ADS_ID__` with the existing `typeof __X__ !== 'undefined' ? __X__ : ''`
    idiom (name it `adsId`). Change the GA4 `window.gtag('config', ga4Id)` call to pass a
    second arg `{ send_page_view: false }`. Immediately after the GA4 config, inside the same
    `if (ga4Id)` block OR a new guarded block, add `if (adsId && window.gtag) window.gtag('config', adsId)`.
    Add exported `fireAdsConversion(label: string, params: Record<string, unknown> = {})`:
    read `adsId` via the same guard; `if (adsId && window.gtag) window.gtag('event', 'conversion',
    { send_to: \`${adsId}/${label}\`, ...params })`. Keep Ads conversion Google-only (do NOT
    dual-dispatch to ym). No new `declare global` needed (Window.gtag already declared).
  </action>
  <verify>
    <automated>npm run lint:ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exports `fireAdsConversion` and references `__GOOGLE_ADS_ID__` + `send_page_view`
    - `grep -c "send_page_view" src/shared/config/analytics/initAnalytics.ts` ≥1
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>Ads configured alongside GA4; conversion helper exported; GA4 page_view suppressed.</done>
</task>

<task type="auto">
  <name>Task 2: initAnalytics unit test (mock gtag)</name>
  <files>src/shared/config/analytics/initAnalytics.test.ts</files>
  <read_first>
    - src/features/Onboarding/lib/onboardingAnalytics.test.ts (whole file — mock/clearAllMocks/it.each idiom)
    - 09-PATTERNS.md § "initAnalytics.test.ts & usePageMeta.test.ts" (do NOT mock initAnalytics; stub window.gtag; set globals per-test)
    - config/jest/jest.config.ts (globals now include the three new constants)
  </read_first>
  <action>
    Create initAnalytics.test.ts. Do NOT mock the module under test. In beforeEach set
    `window.gtag = jest.fn()` and `window.dataLayer = []`; in afterEach reset the build-constant
    globals. Because the constants are `declare const` provided by jest `globals`, override them
    per-test via `(global as any).__GOOGLE_ADS_ID__ = 'AW-16711221644'` /
    `(global as any).__GA4_MEASUREMENT_ID__ = 'G-G1KZQCKP5D'` /
    `(global as any).__ADS_SIGNUP_LABEL__ = '-B6_CK72wtMcEIyDxKA-'` before importing-invoking.
    Assert: initAnalytics calls `gtag('config','G-G1KZQCKP5D',{send_page_view:false})` and
    `gtag('config','AW-16711221644')`; fireAdsConversion('-B6_CK72wtMcEIyDxKA-') calls
    `gtag('event','conversion',{send_to:'AW-16711221644/-B6_CK72wtMcEIyDxKA-'})`. Add a
    no-op test: with `__GOOGLE_ADS_ID__=''`, fireAdsConversion does not call gtag.
  </action>
  <verify>
    <automated>npm run test:unit -- initAnalytics</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- initAnalytics` passes
    - Covers Ads config, GA4 send_page_view:false, conversion send_to, and no-op-when-unset (D-06/D-07)
  </acceptance_criteria>
  <done>Analytics extension proven by green mock-gtag test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → googletagmanager.com | third-party gtag.js script load |
| client → GA4/Ads event params | analytics payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering (supply chain) | gtag.js load | mitigate | Script loaded only from official `https://www.googletagmanager.com/gtag/js` (existing loadScript); no third-party mirror. |
| T-09-06 | Information disclosure | conversion/event params | mitigate | fireAdsConversion sends only the Ads label + non-PII params; do NOT pass email/tokens. Documented in action. |
| T-09-07 | Compliance | EEA consent | accept (flag) | Consent Mode v2 out of scope this plan; recommended in 09-08 SEO audit (Pitfall 7). |
</threat_model>

<verification>
- `npm run test:unit -- initAnalytics` green.
- `npm run lint:ts` green.
</verification>

<success_criteria>
initAnalytics configures GA4 (send_page_view:false) + Ads and exports a guarded
`fireAdsConversion`; no-op when env unset; proven by unit test.
</success_criteria>

<output>
Create `.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-03-SUMMARY.md` when done
</output>
