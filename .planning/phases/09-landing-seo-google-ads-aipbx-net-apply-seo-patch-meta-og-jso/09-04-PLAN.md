---
phase: 09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/pages/MainPage/ui/MainPage.tsx
  - src/pages/VoiceAssistantsLandingPage/ui/VoiceAssistantsLandingPage.tsx
  - src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx
  - public/index.html
  - public/locales/en/main.json
  - public/locales/ru/main.json
autonomous: true
requirements: [D-01, D-08, D-09]
must_haves:
  truths:
    - "All 3 landing pages + MainPage set title/description via t('...meta.*') from the main namespace (EN + RU both present)"
    - "Each landing page passes a per-page SoftwareApplication JSON-LD to usePageMeta (i18n schemaName)"
    - "Each landing page dispatches 'seo-render-ready' after i18n ready (useSeoRenderReady)"
    - "SpeechAnalytics METRICS names render from i18n (EN + RU); color stays in code"
    - "A demo CTA is present on all 3 landing pages with i18n labels"
    - "public/index.html has description/robots/canonical/OG/Organization JSON-LD and an accessible viewport (no user-scalable=no / maximum-scale)"
  artifacts:
    - path: "src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx"
      provides: "i18n meta + JSON-LD + render-ready + METRICS i18n + demo CTA"
      contains: "meta.title"
    - path: "public/index.html"
      provides: "base meta/OG/canonical/Organization JSON-LD + fixed viewport"
      contains: "application/ld+json"
    - path: "public/locales/en/main.json"
      provides: "EN meta/metrics/demoCta keys"
      contains: "meta"
    - path: "public/locales/ru/main.json"
      provides: "RU meta/metrics/demoCta keys (RU preserved)"
      contains: "meta"
  key_links:
    - from: "src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx"
      to: "public/locales/{en,ru}/main.json"
      via: "t('SpeechAnalyticsPage.meta.*') / t('SpeechAnalyticsPage.metrics.*')"
      pattern: "SpeechAnalyticsPage.meta"
    - from: "landing pages"
      to: "seo-render-ready"
      via: "useSeoRenderReady(ready)"
      pattern: "useSeoRenderReady"
---

<objective>
Apply the SEO patch as a BASELINE, not verbatim (D-01): route all landing meta through
i18n so EN and RU coexist (never overwrite RU). On `/`, `/voice-assistants`,
`/speech-analytics` (+ MainPage): drive `usePageMeta` title/description from
`t('...meta.*')`, attach per-page `SoftwareApplication` JSON-LD, and dispatch
`seo-render-ready` after i18n `ready`. Ship the CRO quick wins (D-08): move the hardcoded
RU `METRICS` array to i18n (EN+RU), add a demo CTA on all 3 pages, and fix the
`index.html` base meta + viewport a11y. Speech Analytics is fully functional in EN while
RU is preserved (D-09).

Purpose: the prerender (09-07) snapshots these pages; meta/JSON-LD/hreflang must be
i18n-correct and present and the render-ready event must fire, or bots see a spinner or
untranslated keys.
Output: 3 landing pages + MainPage + index.html + en/ru main.json.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-RESEARCH.md
@.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-PATTERNS.md
@scripts/aipbx_seo.patch
@src/pages/MainPage/ui/MainPage.tsx
@src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx
</context>

## Artifacts this phase produces (this plan)

- New i18n keys (both `en/main.json` AND `ru/main.json`; exact key names are Claude's discretion, keep en/ru parity):
  `MainPage.meta.{title,description,schemaName}`,
  `VoiceAssistantsPage.meta.{title,description,schemaName}`,
  `SpeechAnalyticsPage.meta.{title,description,schemaName}`,
  `SpeechAnalyticsPage.metrics.*` (one key per current METRIC),
  `landing.demoCta.{label,subtitle}`
- Per-page `SoftwareApplication` JSON-LD passed to `usePageMeta`
- `seo-render-ready` dispatch wired on all 3 landing pages (via `useSeoRenderReady`)
- `public/index.html` base meta/OG/canonical/Organization JSON-LD + fixed viewport

<tasks>

<task type="auto">
  <name>Task 1: i18n-driven meta + per-page JSON-LD + render-ready on all landing pages</name>
  <files>src/pages/MainPage/ui/MainPage.tsx, src/pages/VoiceAssistantsLandingPage/ui/VoiceAssistantsLandingPage.tsx, src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx</files>
  <read_first>
    - src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx (meta call lines 40-44)
    - src/pages/VoiceAssistantsLandingPage/ui/VoiceAssistantsLandingPage.tsx (usePageMeta call + inline JSON-LD from patch)
    - src/pages/MainPage/ui/MainPage.tsx (usePageMeta domain-conditional lines 78-89)
    - src/shared/lib/seo/usePageMeta.ts (extended shape from 09-02: ogImage, jsonLd)
    - src/shared/lib/seo/useSeoRenderReady.ts (from 09-02)
    - 09-PATTERNS.md § "Landing pages" ; 09-RESEARCH.md § "Code Examples" + § "Pattern 1/3"
  </read_first>
  <action>
    In each page change `const { t } = useTranslation('main')` to `const { t, ready } = useTranslation('main')`.
    Replace hardcoded meta strings in `usePageMeta({...})` with `title: t('<Page>.meta.title')`,
    `description: t('<Page>.meta.description')`, and add `jsonLd` = a `SoftwareApplication` object
    (`'@context':'https://schema.org'`, `'@type':'SoftwareApplication'`, `name: t('<Page>.meta.schemaName')`,
    `applicationCategory:'BusinessApplication'`, `operatingSystem`, and a `url` built from the route path).
    For MainPage, remove the `isRuDomain` meta ternary and use the i18n key (i18n already resolves per
    active language, so RU comes from ru/main.json — do NOT inline EN over RU). REMOVE the patch's inline
    `dangerouslySetInnerHTML` JSON-LD blocks from the Voice/Speech pages (JSON-LD now flows through
    usePageMeta — single source of truth, no duplicate schema). Add `useSeoRenderReady(ready)` in each page
    so `seo-render-ready` fires once i18n is ready. `<Page>` = `MainPage` | `VoiceAssistantsPage` | `SpeechAnalyticsPage`.
  </action>
  <verify>
    <automated>npm run lint:ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "meta.title" src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx src/pages/VoiceAssistantsLandingPage/ui/VoiceAssistantsLandingPage.tsx src/pages/MainPage/ui/MainPage.tsx` lists all three
    - No hardcoded Russian meta string literals remain in the 3 usePageMeta calls
    - Each page calls `useSeoRenderReady`
    - No `dangerouslySetInnerHTML` JSON-LD blocks remain in the two landing pages
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>Meta + JSON-LD are i18n-driven; render-ready dispatched; RU preserved.</done>
</task>

<task type="auto">
  <name>Task 2: METRICS to i18n, demo CTA, and en/ru locale keys</name>
  <files>src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx, public/locales/en/main.json, public/locales/ru/main.json</files>
  <read_first>
    - src/pages/SpeechAnalyticsLandingPage/ui/SpeechAnalyticsLandingPage.tsx (METRICS array lines 25-35; existing render ~lines 197-201)
    - public/locales/en/main.json + public/locales/ru/main.json (existing main namespace structure)
    - 09-RESEARCH.md § "METRICS to i18n" ; 09-PATTERNS.md § "i18n-driven copy" (hard rule: en+ru parity)
  </read_first>
  <action>
    Change `METRICS` entries from `{ name, color }` to `{ key, color }` with camelCase ids for the 9
    current metrics in order (e.g. greetingQuality, scriptAdherence, politenessEmpathy, activeListening,
    objectionHandling, productKnowledge, problemResolution, speechPace, closingQuality). At render use
    `t('SpeechAnalyticsPage.metrics.' + m.key)` instead of the raw name. Add a demo CTA element
    (button/link, LandingStyles.module.scss classes; link to `getRouteSignup()` or an existing
    demo/playground route) to each of the 3 landing pages with label `t('landing.demoCta.label')` (GAP-42/D-08).
    Add ALL new keys to BOTH en/main.json AND ru/main.json: the `meta.{title,description,schemaName}` groups
    for the 3 pages, the `SpeechAnalyticsPage.metrics.*` map, and `landing.demoCta.{label,subtitle}`.
    EN = English marketing copy (source from patch EN strings + RESEARCH keyword table); RU = the existing
    original Russian strings (reuse the RU text currently hardcoded — do NOT translate to English). NEVER
    delete or overwrite an existing RU value.
  </action>
  <verify>
    <automated>npm run test:unit -- SpeechAnalytics 2>&1 | tail -20 || npm run lint:ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SpeechAnalyticsPage.meta" public/locales/en/main.json` and `... public/locales/ru/main.json` both ≥1
    - `grep -c "demoCta" public/locales/en/main.json public/locales/ru/main.json` both ≥1
    - METRICS render via `t('SpeechAnalyticsPage.metrics.` (no raw RU names in JSX)
    - Every new key exists in BOTH en and ru main.json (no missing-key warnings)
    - `npm run lint:ts` passes
  </acceptance_criteria>
  <done>METRICS + demo CTA + meta copy are i18n (en+ru); RU intact.</done>
</task>

<task type="auto">
  <name>Task 3: index.html base meta, viewport a11y fix, Organization JSON-LD</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (viewport tag; head)
    - scripts/aipbx_seo.patch (index.html hunk — but do NOT hardcode gtag)
    - 09-RESEARCH.md § "index.html base meta + viewport fix" and § "Anti-Patterns" (no hardcoded gtag)
  </read_first>
  <action>
    Fix the viewport meta to `content="width=device-width, initial-scale=1.0"` (REMOVE
    `user-scalable=no, maximum-scale=1.0, minimum-scale=1.0` — Lighthouse a11y, D-08). Add site-wide base
    tags in the head: `<meta name="description">`, `<meta name="robots" content="index, follow">`,
    `<link rel="canonical" href="https://aipbx.net/">`, OG tags (`og:title`, `og:description`, `og:type`,
    `og:url`, `og:site_name`, `og:image` = `https://aipbx.net/assets/og-default.png`), and a site-level
    `Organization` JSON-LD `<script type="application/ld+json">`. Do NOT add the `gtag`/Google Ads
    `<script>` blocks from the patch — analytics is owned by initAnalytics (09-03). Keep the existing
    Google GSI and Telegram widget scripts.
  </action>
  <verify>
    <automated>grep -q 'name="description"' public/index.html && grep -q 'rel="canonical"' public/index.html && grep -q 'application/ld+json' public/index.html && ! grep -q 'user-scalable=no' public/index.html && ! grep -q 'googletagmanager.com/gtag' public/index.html && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - index.html has description, robots, canonical, OG tags, and Organization JSON-LD
    - viewport no longer contains `user-scalable=no` or `maximum-scale`
    - index.html does NOT contain a hardcoded `googletagmanager.com/gtag` script (anti-pattern)
  </acceptance_criteria>
  <done>Static base meta present; viewport accessible; no hardcoded gtag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/i18n content → rendered HTML | meta + JSON-LD strings from code and locale files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering/XSS | JSON-LD in index.html + per-page | accept | Schema strings are static developer-authored content (no user input). ASVS V5 satisfied. |
| T-09-09 | Spoofing | canonical/og:url host | mitigate | index.html canonical/og:url hardcoded to https://aipbx.net; per-page canonical from __SITE_URL__ (09-02). |
</threat_model>

<verification>
- `npm run lint:ts` green.
- `npm run test:unit -- SpeechAnalytics` (if a render test exists) green.
- index.html grep gate (Task 3) prints PASS.
- New keys present in both en and ru main.json.
</verification>

<success_criteria>
All landing meta/JSON-LD/METRICS/demo-CTA are i18n-driven with EN+RU parity, render-ready
fires, and index.html has accessible base meta — RU never broken (D-01/D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/09-landing-seo-google-ads-aipbx-net-apply-seo-patch-meta-og-jso/09-04-SUMMARY.md` when done
</output>
