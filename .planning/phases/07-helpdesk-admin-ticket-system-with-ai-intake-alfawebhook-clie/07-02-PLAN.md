---
phase: 07-helpdesk-admin-ticket-system-with-ai-intake-alfawebhook-clie
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - aiPBX_backend/src/api-keys/api-key.service.ts
  - aiPBX_backend/src/api-keys/api-key-scope.decorator.ts
  - aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.ts
  - aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.module.ts
  - aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.spec.ts
  - aiPBX_backend/src/helpdesk/helpdesk-tools.controller.ts
  - aiPBX_backend/src/helpdesk/helpdesk-llm-context.service.ts
  - aiPBX_backend/src/helpdesk/helpdesk-notification.service.ts
  - aiPBX_backend/src/helpdesk/helpdesk-pbx-agent.service.ts
  - aiPBX_backend/src/helpdesk/helpdesk.module.ts
autonomous: true
must_haves:
  truths:
    - "API key scope helpdesk:tools required on AI tool endpoints"
    - "Built-in ai-tools handlers serve core helpdesk tools without webhook URL"
    - "LLM context regenerates on ticket lifecycle events as hybrid JSON + Markdown"
    - "Email and Telegram fire on new unassigned tickets entering pool"
    - "PBX agent proxy can read vpbx_users and execute promised payment write"
    - "D-03: Ticket created even when client not found; transfer_call documented for unidentified human requests"
    - "D-04: callerPhone stores original Caller ID; contactPhone stores alternate if provided"
    - "D-07: getVpbxUser returns debitingday; blocked=1 explained in bot context"
    - "D-11: Per-client PBX endpoint via helpdesk_pbx_connections.url (cloud or on-prem)"
  artifacts:
    - path: aiPBX_backend/src/helpdesk/helpdesk-tools.controller.ts
      provides: ApiKeyGuard AI endpoints
    - path: aiPBX_backend/src/helpdesk/helpdesk-llm-context.service.ts
      provides: LLM context hybrid storage
---

<objective>
AI intake layer: ApiKey-scoped tool endpoints, built-in handlers in AiToolsHandlersService, LLM context service, notifications, PBX agent proxy (D-06–D-09, D-14–D-17, D-22–D-28, D-32–D-34).

Purpose: Voice and chat assistants can identify clients, manage tickets, fetch LLM context, run PBX diagnostics; operators get notified.
Output: Tools callable via assistant config; tickets auto-created/updated from AI; notifications on pool insert.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-07-03 | Missing scope on tool endpoint | high | @RequireApiKeyScope('helpdesk:tools') on every helpdesk-tools route |
| T-07-04 | Notification leaks full transcript | medium | Truncate body; include ticket link only |
| T-07-05 | SSRF via pbxUrl proxy | high | Validate URL host against allowlist or alfawebhook client record |
| T-07-06 | Destructive PBX write without audit | medium | Log all write ops; require action param confirm=true from tool args |
</threat_model>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-helpdesk-admin-ticket-system-with-ai-intake-alfawebhook-clie/07-CONTEXT.md
@.planning/phases/07-helpdesk-admin-ticket-system-with-ai-intake-alfawebhook-clie/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API key scope helpdesk:tools (D-28)</name>
  <files>aiPBX_backend/src/api-keys/api-key.service.ts, aiPBX_backend/src/api-keys/api-key-scope.decorator.ts</files>
  <read_first>aiPBX_backend/src/api-keys/api-key.service.ts, aiPBX_backend/src/ai-models/ai-models.controller.ts</read_first>
  <action>
Add HELPDESK_TOOLS: 'helpdesk:tools' to API_KEY_SCOPES constant.
Export in api-key-scope.decorator if needed for decorator usage.
  </action>
  <acceptance_criteria>
    - API_KEY_SCOPES.HELPDESK_TOOLS equals 'helpdesk:tools'
    - ApiKeyService.validate rejects key without scope when required
  </acceptance_criteria>
  <verify>
    <automated>grep "helpdesk:tools" aiPBX_backend/src/api-keys/api-key.service.ts</automated>
  </verify>
  <done>Scope constant available (D-28)</done>
</task>

<task type="auto">
  <name>Task 2: HelpdeskToolsController — AI endpoints (D-27, D-28)</name>
  <files>aiPBX_backend/src/helpdesk/helpdesk-tools.controller.ts, aiPBX_backend/src/helpdesk/helpdesk.module.ts</files>
  <read_first>aiPBX_backend/src/ai-models/ai-models.controller.ts, aiPBX_backend/src/helpdesk/helpdesk.service.ts, 07-CONTEXT.md D-27</read_first>
  <action>
Create HelpdeskToolsController @Controller('helpdesk/tools') with @UseGuards(ApiKeyGuard) and @RequireApiKeyScope(API_KEY_SCOPES.HELPDESK_TOOLS) on class level.
Endpoints (POST, JSON body):
- identify-client { phone?, inn?, name? }
- get-client-info { clientId or inn }
- get-llm-context { clientId or inn }
- create-ticket { subject, category, priority, callerPhone, contactPhone, clientRef?, transcript? } — status new, assigneeId null (D-23 call start). D-03: allow create with clientName only when not found. D-04: callerPhone=original Caller ID; contactPhone=alternate if different.
- add-message { ticketId, role, content }
- pbx-get-vpbx-user { clientId }
- pbx-list-sip-registrations { clientId }
- pbx-promised-payment { clientId, days } — max 5 per D-08
- pbx-hangup-channel { clientId, channelId, confirm: true }
Use req.apiKeyUserId for createdByApiKeyId audit.
  </action>
  <acceptance_criteria>
    - helpdesk-tools.controller.ts exists with ApiKeyGuard
    - create-ticket creates ticket with assigneeId null
    - openapi.json updated with helpdesk/tools paths
  </acceptance_criteria>
  <verify>
    <automated>cd aiPBX_backend && npm run openapi:export && grep "helpdesk/tools" openapi.json</automated>
  </verify>
  <done>REST AI tool endpoints live (D-27, D-28)</done>
</task>

<task type="auto">
  <name>Task 3: Built-in handlers in AiToolsHandlersService (D-27)</name>
  <files>aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.ts, aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.module.ts, aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.spec.ts</files>
  <read_first>aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.ts, aiPBX_backend/src/ai-tools-handlers/ai-tools-handlers.service.spec.ts knowledge_base tests</read_first>
  <action>
Import HelpdeskModule or inject HelpdeskToolFacade service to avoid circular deps (extract HelpdeskToolsFacade if needed).
In functionHandler after knowledge_base check, if toolData.handler starts with helpdesk_ route to handleHelpdeskTool(handler, parsedArgs, assistant).
Map handlers: helpdesk_identify_client, helpdesk_get_client_info, helpdesk_get_llm_context, helpdesk_create_ticket, helpdesk_add_message, helpdesk_pbx_get_vpbx_user, helpdesk_pbx_promised_payment, etc. — delegate to same logic as HelpdeskToolsController.
Extend ai-tools-handlers.service.spec.ts with helpdesk handler routing test.
Import HelpdeskModule in AiToolsHandlersModule.
  </action>
  <acceptance_criteria>
    - toolData.handler === 'helpdesk_identify_client' routes without webhook
    - Spec test passes for helpdesk handler routing
    - Webhook path still works for non-built-in tools
  </acceptance_criteria>
  <verify>
    <automated>cd aiPBX_backend && npm test -- --testPathPattern=ai-tools-handlers.service.spec</automated>
  </verify>
  <done>Hybrid built-in + webhook intake (D-27)</done>
</task>

<task type="auto">
  <name>Task 4: LLM context service (D-14–D-17)</name>
  <files>aiPBX_backend/src/helpdesk/helpdesk-llm-context.service.ts</files>
  <read_first>07-CONTEXT.md D-14 D-15 D-16 D-17, aiPBX_backend/src/helpdesk/models/helpdesk-client-context.model.ts</read_first>
  <action>
HelpdeskLlmContextService methods:
- getContext(clientKey) — returns { summaryMarkdown, rawMarkdown, json }
- upsertFromTicketEvent(ticket) — merge ticket into contextJson; regenerate contextMarkdown from template
- updateOperatorOverride(clientKey, markdownOverride) — save contextMarkdownOverride
Auto-update hooks called from HelpdeskService on: ticket create, status→resolved/closed, operator message add, pbx diagnostics complete (D-15).
get-llm-context tool returns rawMarkdown for bot (D-17 — not system prompt injection).
  </action>
  <acceptance_criteria>
    - helpdesk-llm-context.service.ts exports regenerateMarkdown from JSON
    - Ticket close triggers upsertFromTicketEvent
    - Unit test covers JSON to Markdown generation
  </acceptance_criteria>
  <verify>
    <automated>cd aiPBX_backend && npm test -- --testPathPattern=helpdesk-llm-context --passWithNoTests</automated>
  </verify>
  <done>Hybrid LLM context storage (D-14–D-17)</done>
</task>

<task type="auto">
  <name>Task 5: Notifications on unassigned pool (D-32–D-34)</name>
  <files>aiPBX_backend/src/helpdesk/helpdesk-notification.service.ts, aiPBX_backend/src/helpdesk/helpdesk.service.ts</files>
  <read_first>aiPBX_backend/src/mailer/mailer.service.ts, aiPBX_backend/src/telegram/telegram.service.ts, 07-CONTEXT.md D-32 D-33 D-34</read_first>
  <action>
HelpdeskNotificationService.notifyNewUnassignedTicket(ticket):
- Load recipients from helpdesk_settings (emails + telegram chat IDs)
- Send only when ticket created with assigneeId null (D-33)
- Email via MailerService; Telegram via TelegramService.sendMessage
- Body: ticket id, client name, category, priority, link to admin UI (truncate transcript)
Wire call from HelpdeskService.create after insert when assigneeId null.
Add GET/PATCH /helpdesk/settings admin endpoints for recipient config (D-34).
  </action>
  <acceptance_criteria>
    - Notification not sent when assigneeId set on create
    - helpdesk_settings stores notificationEmails array
    - MailerService invoked with subject containing ticket id
  </acceptance_criteria>
  <verify>
    <automated>cd aiPBX_backend && npm test -- --testPathPattern=helpdesk-notification --passWithNoTests</automated>
  </verify>
  <done>Email+TG notifications (D-32–D-34)</done>
</task>

<task type="auto">
  <name>Task 6: PBX agent proxy service (D-06–D-13)</name>
  <files>aiPBX_backend/src/helpdesk/helpdesk-pbx-agent.service.ts</files>
  <read_first>07-CONTEXT.md D-06 D-08 D-10 D-13, aiPBX_backend/src/helpdesk/models/helpdesk-pbx-connection.model.ts, aiPBX_backend/src/vpbx_users/vpbx_users.model.ts</read_first>
  <action>
HelpdeskPbxAgentService:
- resolveConnection(clientId) — load helpdesk_pbx_connections by alfawebhookClientId; each row has per-client url (cloud or on-prem) per D-11; optional fleet proxy URL as separate connection row, not required
- proxyGet(path) — HTTP GET to agent with decrypted apiKey header X-Api-Key
- getVpbxUser — return balance, debitingday, blocked, licnum; document blocked=1 only blocking (D-06); include debitingday in response for bot billing context (D-07)
- promisedPayment(days) — POST to agent; clamp days 2-5 default 2 (D-08)
- listSipRegistrations, hangupChannel (require confirm=true in args D-13)
Encrypt/decrypt apiKey using HELPdesk_ENCRYPTION_KEY env (T-07-02).
  </action>
  <acceptance_criteria>
    - getVpbxUser returns blocked field from agent response
    - promisedPayment rejects days &gt; 5
    - apiKey not logged in plaintext
  </acceptance_criteria>
  <verify>
    <automated>cd aiPBX_backend && npm test -- --testPathPattern=helpdesk-pbx-agent --passWithNoTests</automated>
  </verify>
  <done>PBX agent proxy ready (D-06–D-13)</done>
</task>

</tasks>

## Artifacts this phase produces

- `API_KEY_SCOPES.HELPDESK_TOOLS` — scope constant `'helpdesk:tools'`
- `HelpdeskToolsController` — ApiKeyGuard REST controller for AI tools
- `HelpdeskLlmContextService` — hybrid JSON/Markdown context service
- `HelpdeskNotificationService` — email/Telegram notification service
- `HelpdeskPbxAgentService` — PBX HTTP agent proxy
- `AiToolsHandlersService.handleHelpdeskTool()` — built-in handler dispatch (private method)
- Helpdesk built-in handler cases in `ai-tools-handlers.service.spec.ts`

## Verification criteria

- [ ] API key without helpdesk:tools scope gets 401 on tool endpoints
- [ ] Built-in handler creates ticket without webhook URL configured
- [ ] LLM context updates after ticket close
- [ ] Notification sent on unassigned ticket create
