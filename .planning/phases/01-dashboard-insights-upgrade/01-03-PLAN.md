---
phase: 01-dashboard-insights-upgrade
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/entities/Report/model/types/report.ts
  - src/entities/Report/api/reportApi.ts
  - src/entities/Report/index.ts
  - src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx
  - src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.module.scss
  - src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx
  - src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx
  - public/locales/en/reports.json
  - public/locales/ru/reports.json
  - public/locales/de/reports.json
  - public/locales/zh/reports.json
autonomous: true
requirements: [REQ-06, REQ-07, REQ-08, REQ-09]
user_setup: []
must_haves:
  truths:
    - "getOperatorInsights returns OperatorInsightsResponse with structured insights array"
    - "AiInsightsBanner shows priority badge and evidence chip per insight"
    - "Admin dashboard passes userId to insights API same as dashboard query"
    - "Changing date/project filters clears prior insights display until user refetches"
  artifacts:
    - path: src/entities/Report/model/types/report.ts
      provides: "OperatorInsight, OperatorInsightsResponse types"
      contains: "OperatorInsight"
    - path: src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx
      provides: "Structured insights UI"
      contains: "lowConfidence"
  key_links:
    - from: src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx
      to: /operator-analytics/insights
      via: "useLazyGetOperatorInsights with userId param"
      pattern: "userId"
    - from: src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx
      to: src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx
      via: "userId prop"
      pattern: "userId"
---

<objective>
Update frontend to consume structured insights: types, API, redesigned banner, admin userId wiring, and i18n.

Purpose: End-to-end structured insights visible in Operator Analytics dashboard.
Output: Typed RTK endpoint, redesigned AiInsightsBanner, locale keys, userId propagation.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-dashboard-insights-upgrade/01-CONTEXT.md
@.planning/phases/01-dashboard-insights-upgrade/01-RESEARCH.md
@.planning/phases/01-dashboard-insights-upgrade/01-02-SUMMARY.md

@src/entities/Report/model/types/report.ts
@src/entities/Report/api/reportApi.ts
@src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx
@src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx
@src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types and RTK API</name>
  <files>src/entities/Report/model/types/report.ts, src/entities/Report/api/reportApi.ts, src/entities/Report/index.ts</files>
  <read_first>src/entities/Report/model/types/report.ts, src/entities/Report/api/reportApi.ts, .planning/phases/01-dashboard-insights-upgrade/01-CONTEXT.md</read_first>
  <action>
Add to `report.ts`:

```typescript
export type InsightPriority = 'high' | 'medium' | 'low'
export type InsightType = 'strength' | 'gap' | 'trend' | 'outlier' | 'quality'

export interface OperatorInsightEvidence {
  metric?: string
  value?: number
  operators?: string[]
  periodLabel?: string
}

export interface OperatorInsight {
  priority: InsightPriority
  type: InsightType
  title: string
  observation: string
  recommendation: string
  evidence: OperatorInsightEvidence
}

export interface OperatorInsightsResponse {
  insights: OperatorInsight[]
  generatedAt: string
  promptVersion: string
  sampleSize: number
  lowConfidence: boolean
  factsDigest?: string
}
```

Update `getOperatorInsights` in `reportApi.ts`:
- Response type: `OperatorInsightsResponse`
- Args type add: `userId?: string`, `refresh?: boolean | string`
- Map `refresh: true` to query param `refresh: '1'`
- Export types from `index.ts` if not re-exported
  </action>
  <verify>npm run build:prod 2>&1 | tail -20 || npx tsc --noEmit 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - report.ts contains `export interface OperatorInsight`
    - reportApi.ts getOperatorInsights uses `OperatorInsightsResponse` not `string[]`
    - reportApi.ts query args type includes `userId`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: userId wiring through component chain</name>
  <files>src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx</files>
  <read_first>src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx</read_first>
  <action>
Pass `userId` from `DashboardCallRecordsContent` to `OperatorDashboard` as new optional prop `userId?: string | number`.

Update `OperatorDashboard` props interface to accept `userId` and pass to `AiInsightsBanner` in `queryParams`:
`{ startDate, endDate, projectId, userId: userId ? String(userId) : undefined }`

Extend `AiInsightsBannerProps.queryParams` with `userId?: string` and optional `operatorName?: string`.

Add `useEffect` in `AiInsightsBanner` that resets displayed insights when `queryParams` change:
- Use RTK `fixedCacheKey` removed OR call reset by not rendering stale `data` when serialized queryParams key changes (store `lastFetchKey` in useRef; if key differs from current, treat `data` as stale and show fetch button again)

Ensure `handleFetch` passes full queryParams including userId to `triggerInsights`.
Add `handleRefresh` calling `triggerInsights({ ...queryParams, refresh: true })`.
  </action>
  <verify>grep -n "userId" src/pages/DashboardCallRecordsPage/ui/DashboardCallRecordsPage/DashboardCallRecordsPage.tsx src/features/OperatorAnalytics/ui/OperatorDashboard/OperatorDashboard.tsx src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx</verify>
  <acceptance_criteria>
    - DashboardCallRecordsPage passes userId prop to OperatorDashboard
    - OperatorDashboard passes userId inside queryParams to AiInsightsBanner
    - AiInsightsBanner.tsx contains useEffect related to queryParams reset
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Banner redesign + i18n + styles</name>
  <files>src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.module.scss, public/locales/en/reports.json, public/locales/ru/reports.json, public/locales/de/reports.json, public/locales/zh/reports.json</files>
  <read_first>src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx, src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.module.scss, public/locales/en/reports.json</read_first>
  <action>
Redesign `AiInsightsBanner` to render `OperatorInsight` objects:
- Header row: title + buttons «Получить инсайты» (when no data) and «Обновить» (when data exists, calls refresh)
- Per insight card:
  - Priority badge: high=red border/background, medium=amber, low=gray (CSS classes `.priorityHigh`, `.priorityMedium`, `.priorityLow`)
  - Type label via i18n key `INSIGHT_TYPE_{{type}}`
  - Bold `title`, then `observation`, then `recommendation`
  - Evidence chip: `{metric}: {value}` or operator names joined
- `lowConfidence` banner when `data.lowConfidence === true`: show `INSIGHTS_LOW_CONFIDENCE` with `sampleSize`
- Error state: when RTK `isError`, show `INSIGHTS_ERROR` (check `error` for balance/402 if available)
- Loading: 3 skeleton rows when `isLoading || isFetching`

Add i18n keys to all 4 locale files under `reports` namespace:
- `INSIGHT_PRIORITY_high`, `INSIGHT_PRIORITY_medium`, `INSIGHT_PRIORITY_low`
- `INSIGHT_TYPE_strength`, `INSIGHT_TYPE_gap`, `INSIGHT_TYPE_trend`, `INSIGHT_TYPE_outlier`, `INSIGHT_TYPE_quality`
- `INSIGHTS_REFRESH`, `INSIGHTS_LOW_CONFIDENCE`, `INSIGHTS_ERROR`, `INSIGHTS_EVIDENCE_METRIC`

Update SCSS: `.insightCard`, `.priorityHigh`, `.priorityMedium`, `.priorityLow`, `.evidenceChip`, `.lowConfidenceBanner`
  </action>
  <verify>grep -E "INSIGHT_PRIORITY_high|INSIGHTS_LOW_CONFIDENCE|lowConfidence" src/features/OperatorAnalytics/ui/OperatorDashboard/AiInsightsBanner/AiInsightsBanner.tsx public/locales/en/reports.json</verify>
  <acceptance_criteria>
    - AiInsightsBanner maps over `insight.title` not plain `text` string
    - en/reports.json contains INSIGHT_PRIORITY_high and INSIGHTS_REFRESH
    - AiInsightsBanner.module.scss contains .evidenceChip or .priorityHigh
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run build:prod` or `tsc --noEmit` passes
- [ ] No remaining `insights: string[]` in reportApi getOperatorInsights definition
</verification>

<success_criteria>
- Frontend types match backend OperatorInsightsResponse
- Banner displays structured insights with priority, type, evidence
- Admin userId reaches insights API
- i18n keys added for en/ru/de/zh
</success_criteria>

## Artifacts this phase produces

| Symbol | Location |
|--------|----------|
| `OperatorInsight`, `OperatorInsightsResponse` | report.ts |
| Updated `getOperatorInsights` endpoint typing | reportApi.ts |
| `userId` prop on `OperatorDashboard` | OperatorDashboard.tsx |
| Priority/evidence UI classes | AiInsightsBanner.module.scss |
| i18n keys `INSIGHT_*` | public/locales/*/reports.json |

<output>
After completion, create `.planning/phases/01-dashboard-insights-upgrade/01-03-SUMMARY.md`
</output>
