---
phase: 01-dashboard-insights-upgrade
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - aiPBX_backend/src/operator-analytics/lib/insights-schema.ts
  - aiPBX_backend/src/operator-analytics/lib/insights-facts.ts
  - aiPBX_backend/src/operator-analytics/lib/insights-prompt.ts
  - aiPBX_backend/src/operator-analytics/lib/insights-schema.spec.ts
  - aiPBX_backend/src/operator-analytics/lib/insights-facts.spec.ts
autonomous: true
requirements: [REQ-01, REQ-02]
user_setup: []
must_haves:
  truths:
    - "OperatorInsight type exports priority, type, title, observation, recommendation, evidence fields"
    - "buildInsightsFacts produces metricRanking and operatorOutliers from dashboard fixture"
    - "buildInsightsPrompt contains Russian-language and grounded-only guardrail phrases"
  artifacts:
    - path: aiPBX_backend/src/operator-analytics/lib/insights-schema.ts
      provides: "Types, zod schema, json_schema builder, parseAndValidateInsightsResponse"
      contains: "INSIGHTS_PROMPT_VERSION"
    - path: aiPBX_backend/src/operator-analytics/lib/insights-facts.ts
      provides: "buildInsightsFacts pure function"
      exports: ["buildInsightsFacts"]
    - path: aiPBX_backend/src/operator-analytics/lib/insights-prompt.ts
      provides: "buildInsightsPrompt"
      exports: ["buildInsightsPrompt"]
  key_links:
    - from: aiPBX_backend/src/operator-analytics/lib/insights-prompt.ts
      to: aiPBX_backend/src/operator-analytics/lib/insights-facts.ts
      via: "imports facts object type"
      pattern: "buildInsightsFacts|InsightsFacts"
---

<objective>
Create the insights domain module: structured types, deterministic facts builder, and grounded LLM prompt with JSON schema.

Purpose: Establish the contract and testable prep layer before service integration.
Output: `insights-schema.ts`, `insights-facts.ts`, `insights-prompt.ts` + unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
@$HOME/.cursor/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-dashboard-insights-upgrade/01-CONTEXT.md
@.planning/phases/01-dashboard-insights-upgrade/01-RESEARCH.md

@aiPBX_backend/src/operator-analytics/lib/analysis-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Schema validation tests</name>
  <files>aiPBX_backend/src/operator-analytics/lib/insights-schema.spec.ts</files>
  <read_first>aiPBX_backend/src/operator-analytics/lib/analysis-schema.ts, .planning/phases/01-dashboard-insights-upgrade/01-CONTEXT.md</read_first>
  <action>
Create `insights-schema.spec.ts` with failing tests:
- valid structured payload passes `parseAndValidateInsightsResponse`
- invalid payload (missing `priority`) throws or returns validation error
- legacy `string[]` input wraps into minimal `OperatorInsight[]` with `type: 'quality'`, `priority: 'low'`
- `INSIGHTS_PROMPT_VERSION` equals `'2026-06-18.1'`
- `buildInsightsJsonSchema()` returns object with `type: 'object'` and `strict: true` (or equivalent OpenAI schema shape used in analysis-schema)
  </action>
  <verify>cd aiPBX_backend && npm test -- insights-schema.spec.ts 2>&1 | head -20</verify>
  <acceptance_criteria>
    - insights-schema.spec.ts exists with at least 4 test cases
    - Test run fails because insights-schema.ts does not exist yet (RED)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implement insights-schema.ts</name>
  <files>aiPBX_backend/src/operator-analytics/lib/insights-schema.ts</files>
  <read_first>aiPBX_backend/src/operator-analytics/lib/analysis-schema.ts, aiPBX_backend/src/operator-analytics/lib/insights-schema.spec.ts</read_first>
  <action>
Create `insights-schema.ts` exporting:
- `INSIGHTS_PROMPT_VERSION = '2026-06-18.1'`
- Types: `InsightPriority`, `InsightType`, `OperatorInsight`, `OperatorInsightsResponse`
- Zod schemas for insight and response
- `buildInsightsJsonSchema()` mirroring analysis-schema OpenAI strict JSON pattern
- `parseAndValidateInsightsResponse(raw: unknown): OperatorInsightsResponse` with legacy fallback: if `raw` is `{ insights: string[] }`, map each string to `{ priority:'low', type:'quality', title: text.slice(0,80), observation: text, recommendation: '', evidence: {} }`
  </action>
  <verify>cd aiPBX_backend && npm test -- insights-schema.spec.ts</verify>
  <acceptance_criteria>
    - insights-schema.spec.ts exits 0
    - insights-schema.ts contains `export const INSIGHTS_PROMPT_VERSION = '2026-06-18.1'`
    - insights-schema.ts contains `parseAndValidateInsightsResponse`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (TDD): Facts builder + prompt module</name>
  <files>aiPBX_backend/src/operator-analytics/lib/insights-facts.ts, aiPBX_backend/src/operator-analytics/lib/insights-facts.spec.ts, aiPBX_backend/src/operator-analytics/lib/insights-prompt.ts</files>
  <read_first>aiPBX_backend/src/operator-analytics/operator-analytics.service.ts, .planning/phases/01-dashboard-insights-upgrade/01-CONTEXT.md</read_first>
  <action>
Implement `buildInsightsFacts(dashboard, project?, query?)` returning typed `InsightsFacts` with blocks:
`summary`, `metricRanking`, `operatorOutliers`, `trends`, `customMetrics`, `dataQuality`, `focusMetrics`, `sampleSize`, `lowConfidence` (true when sampleSize &lt; OPERATOR_INSIGHTS_MIN_CALLS default 10).

Sources from dashboard payload:
- `aggregatedMetrics` → metricRanking (worst/best by value)
- `agentScorecards` → operatorOutliers (bottom/top by avg score, min n=3 per operator)
- `timeSeries.daily` or `monthly` → trends (first vs last period delta for avgScore)
- `customMetricsAggregated` → customMetrics summary
- `excludedLowQualityCount` → dataQuality
- `project.visibleDefaultMetrics` or `visibleDefaultMetrics` → focusMetrics filter list

Implement `buildInsightsPrompt(facts, projectContext?)` returning `{ system: string, user: string }` with rules:
- "Respond only in JSON"
- "Use ONLY provided facts"
- "3-6 insights"
- "Russian for title, observation, recommendation"
- "Each insight must include evidence.metric and evidence.value when applicable"
- "If lowConfidence, include type quality insight about small sample"

Add `insights-facts.spec.ts` with fixture dashboard JSON asserting worst metric name, operator outlier name, and `lowConfidence: true` when n=4.
  </action>
  <verify>cd aiPBX_backend && npm test -- insights-facts.spec.ts insights-schema.spec.ts</verify>
  <acceptance_criteria>
    - insights-facts.spec.ts exits 0
    - buildInsightsPrompt output (system+user) contains substring `ONLY provided facts`
    - buildInsightsPrompt output contains substring `Russian`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd aiPBX_backend && npm test -- insights-schema insights-facts` exits 0
- [ ] No imports from operator-analytics.service.ts into facts module (pure functions only)
</verification>

<success_criteria>
- Schema module validates structured and legacy responses
- Facts builder is pure and unit-tested
- Prompt module encodes grounded-generation guardrails
</success_criteria>

## Artifacts this phase produces

| Symbol | Location |
|--------|----------|
| `INSIGHTS_PROMPT_VERSION` | insights-schema.ts |
| `OperatorInsight`, `OperatorInsightsResponse` | insights-schema.ts |
| `parseAndValidateInsightsResponse` | insights-schema.ts |
| `buildInsightsJsonSchema` | insights-schema.ts |
| `buildInsightsFacts` | insights-facts.ts |
| `InsightsFacts` (type) | insights-facts.ts |
| `buildInsightsPrompt` | insights-prompt.ts |

<output>
After completion, create `.planning/phases/01-dashboard-insights-upgrade/01-01-SUMMARY.md`
</output>
