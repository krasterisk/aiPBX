---
phase: 10-speech-analytics-ux-overhaul-operator-drill-down-call-tags-t
plan: 10
type: execute
wave: 6
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05, 10-06, 10-07, 10-08, 10-09]
autonomous: false
requirements: [D-02, D-27, D-30]
files_modified:
  - aiPBX/public/locales/ru/reports.json
  - aiPBX/public/locales/en/reports.json
  - aiPBX/public/locales/de/reports.json
  - aiPBX/public/locales/zh/reports.json
  - aiPBX/src/shared/lib/i18n/reportsLocaleParity.test.ts
  - aiPBX_backend/openapi.json
  - aiPBX/src/shared/api/openapi.json
  - aiPBX/src/shared/api/generated/schema.d.ts
  - aiPBX/.planning/intel/API-MAP.md
  - aiPBX/.planning/GAPS.md

must_haves:
  truths:
    - "Every user-visible string introduced by the phase resolves in all four shipped locales, so a German or Chinese manager does not read Russian source strings in a new panel."
    - "A key added to one locale but missed in another fails an automated check rather than shipping as a silent fallback."
    - "The committed API contract matches the code: regenerating it from the backend produces no diff, and regenerating the frontend types from that contract produces no diff."
    - "The three new endpoints and two new query parameters are recorded in the API map, so the next phase does not rediscover them by reading controllers."
    - "The follow-up findings this phase surfaced but deliberately did not fix are written into the backlog rather than lost in a summary."
    - "A human has confirmed the four things no automated test in this phase can see: panel widths at three viewports, section density, «Темы» grid behaviour at three theme counts, and whether the evidence quote clamp is deep enough to be convincing."
    - "A human has confirmed that analysing a real call against a real project taxonomy produces the tags the manager expected — the one behaviour that spans the LLM pipeline, the matcher, the database and three UI surfaces."
  artifacts:
    - "phase copy present in all four locale report namespaces"
    - "aiPBX/src/shared/lib/i18n/reportsLocaleParity.test.ts — automated four-locale key-parity gate for the phase's keys"
    - "regenerated openapi.json in both repos and regenerated frontend schema types"
    - "API map rows for the new endpoints and parameters"
    - "backlog entries for the deferred findings"
  key_links:
    - "new copy uses the Russian source string as its key, so the Russian locale is correct by construction and the other three locales are what this plan actually adds"
    - "the contract regeneration order is backend export, then frontend sync, then type generation — running them out of order produces a diff that looks like a code problem"
    - "both repos ship a check variant of their generation script that fails on a diff; those are the gates, not the generation scripts themselves"
---

<objective>
Close the phase: translate everything it added into the three non-Russian locales behind an automated parity gate, bring the committed API contract and generated types back in step with the code, record the new API surface and the deferred findings, and put the handful of genuinely visual questions in front of a human.

Purpose: the phase adds two new panels, a new section, chips on three surfaces and three endpoints. Each of the preceding plans is green on its own tests, but four things are only true at the phase level: locale parity, contract freshness, the recorded API surface, and whether the result actually looks and reads the way it was designed to.
Output: four locale files, one parity test, regenerated contract artifacts, two updated planning documents, and one human sign-off.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-speech-analytics-ux-overhaul-operator-drill-down-call-tags-t/10-CONTEXT.md
@.planning/phases/10-speech-analytics-ux-overhaul-operator-drill-down-call-tags-t/10-UI-SPEC.md
@.planning/phases/10-speech-analytics-ux-overhaul-operator-drill-down-call-tags-t/10-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the phase reads correctly in four languages" — copy plus an automated parity gate</name>
  <files>aiPBX/public/locales/ru/reports.json, aiPBX/public/locales/en/reports.json, aiPBX/public/locales/de/reports.json, aiPBX/public/locales/zh/reports.json, aiPBX/src/shared/lib/i18n/reportsLocaleParity.test.ts</files>
  <read_first>
    - `aiPBX/public/locales/ru/reports.json` and `aiPBX/public/locales/en/reports.json` — the two conventions in use side by side: most keys are the Russian source string itself, a minority are screaming-snake identifiers
    - `.planning/phases/10-.../10-0{2,3,4,6,8,9}-SUMMARY.md` — the actual copy shipped by each plan, which is the authoritative list of keys to translate
    - 10-UI-SPEC.md § Copywriting Contract — the Russian and English wording for every empty state, error state, notice and control label the phase introduced
    - `aiPBX/config/jest/jest.config.ts` — the test roots and module resolution, which determine how the test reads the locale files
    - `.cursor/rules/aipbx-core.mdc` — the definition of done requiring Russian and English at minimum for user-facing copy
  </read_first>
  <behavior>
    - Every phase key present in the Russian namespace is present in the English, German and Chinese namespaces.
    - A key added to one locale and missing from another fails the test with a message naming the locale and the key.
    - All four namespace files remain valid JSON with the repository's existing formatting.
    - The English wording for every empty state, error state and notice matches the design contract.
    - Keys carrying an interpolated count keep the same placeholder name in all four locales.
  </behavior>
  <action>
    Walk the summaries of the preceding plans and collect every user-visible string the phase introduced across the panel chrome, the operator panel, the theme panel, the «Темы» section and both of its empty states, the chip row and its no-themes note, the taxonomy editor and its deletion confirmation, the capped-sample notice, the panel error state and the tag-save failure message. Because new copy uses the Russian source string as its key, the Russian namespace is correct by construction — what this plan adds is the other three locales. Take the English wording from the design contract's copywriting table rather than inventing it, and translate German and Chinese from that English. Keep interpolation placeholders identical across all four locales; a renamed placeholder degrades to a literal brace expression on screen, which is worse than an untranslated string.

    Then write the parity test: read the four report namespaces, take the phase's key list, and assert each key resolves in each locale, failing with a message that names both the locale and the missing key. Keep the phase key list in the test file itself so the gate is explicit about what it covers rather than silently passing on an empty set.
  </action>
  <verify>
    <automated>npx jest --config config/jest/jest.config.ts src/shared/lib/i18n/reportsLocaleParity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behaviours above have a passing assertion.
    - The phase's key list in the test is non-empty and covers the panel chrome, both panel bodies, the section and both its empty states, the chip row, the taxonomy editor, the capped notice and both error messages.
    - Interpolation placeholder names are identical across the four locales.
    - All four files parse as JSON.
  </acceptance_criteria>
  <done>Every string the phase added resolves in Russian, English, German and Chinese, and a future missing translation fails a test instead of shipping.</done>
</task>

<task type="auto">
  <name>Task 2: Bring the committed contract, the API map and the backlog back in step</name>
  <files>aiPBX_backend/openapi.json, aiPBX/src/shared/api/openapi.json, aiPBX/src/shared/api/generated/schema.d.ts, aiPBX/.planning/intel/API-MAP.md, aiPBX/.planning/GAPS.md</files>
  <read_first>
    - `aiPBX_backend/package.json` and `aiPBX/package.json` — the export, sync, type-generation scripts and their check variants, including which artifact each one writes
    - `aiPBX/.planning/intel/API-MAP.md` — the row format for an endpoint entry and where the operator-analytics routes are listed
    - `aiPBX/.planning/GAPS.md` — the backlog item format and its priority convention
    - `.planning/phases/10-.../10-01-SUMMARY.md`, `10-05-SUMMARY.md`, `10-09-SUMMARY.md` — the shipped endpoints, the new query parameters, and the deferred findings to record
  </read_first>
  <behavior>
    - Regenerating the backend contract leaves no uncommitted difference.
    - Syncing that contract into the frontend and regenerating the types leaves no uncommitted difference.
    - The generated types include the new response and payload shapes.
    - The API map lists the evidence endpoint, the manual tag-update endpoint, and the two new query parameters on the analysed-calls route.
    - The backlog carries an entry for the export formula-safety of the pre-existing columns and one for the evidence sample-size tuning knobs.
  </behavior>
  <action>
    Run the generation chain in order — export the contract from the backend, sync it into the frontend, then regenerate the frontend types — and commit all three artifacts. Order matters: generating types before syncing produces a diff that reads like a code defect and sends the next person hunting in the wrong repo. Then confirm with each repo's check variant of the script, which is the gate that fails on a diff; the generation scripts themselves always succeed and prove nothing.

    Add the new routes and parameters to the API map with the same row format the existing operator-analytics entries use, including which caller roles reach them, so the next phase reads the map instead of the controllers. Add two backlog entries: the spreadsheet formula-safety of the export's pre-existing columns, which 10-09 deliberately scoped out, and the evidence sample-size and per-metric evidence-count knobs from 10-01, which the human verification below may want changed.
  </action>
  <verify>
    <automated>npm run generate:api-types:check</automated>
  </verify>
  <acceptance_criteria>
    - Both repos' contract check scripts pass with no diff.
    - The generated frontend types contain the new response and payload shapes.
    - The API map lists both new endpoints and both new query parameters with their roles.
    - Two backlog entries are recorded with priorities.
  </acceptance_criteria>
  <done>The committed contract, the generated frontend types, the API map and the backlog all match what the phase actually shipped.</done>
</task>

  <task type="checkpoint:human-verify">
  <name>Task 3: Human verification of what no test in this phase can see</name>
  <files>none</files>
  <read_first>
    - 10-VALIDATION.md § Manual-Only Verifications — the full list with the reason each one is manual
    - 10-UI-SPEC.md § UI Considerations — the three backstop rows and the one unresolved row
    - `.planning/phases/10-.../10-06-SUMMARY.md` — where the evidence quote clamp value was placed, so a change is a one-line edit
  </read_first>
  <action>
    Present the checks below to the user and record the outcome. Nothing here is verifiable in the test environment: the first four are CSS media queries, grid reflow and text truncation, and the last spans the LLM analysis pipeline, the matcher, the database and three UI surfaces at once.

    Report the result in the plan summary. If the quote clamp is judged too shallow, change the single stylesheet value and note it; if the evidence sample cap or the per-metric evidence count needs tuning, record the chosen values against the backlog entry from Task 2 rather than changing them silently.
  </action>
  <what-built>
    Phase 10 OA UX: SidePanel drill-down (operator → metric → evidence → calls), Themes section with tag stats, project taxonomy tagging (auto + manual), dashboard cleanup (Usage / Динамика / Активность removed; Insights after Stats), tag chips on journal/card/CSV, i18n + OpenAPI contract regen.
  </what-built>
  <how-to-verify>
    On a running instance confirm:

    1. **Panel width (D-30)** — open an operator drill-down at roughly 375px, 800px and 1440px viewport widths. Expected: fullscreen on the phone width, a narrow sheet at the tablet width, and a comfortable sheet capped around half the content column on the desktop width.
    2. **Section density (D-27)** — scroll the dashboard top to bottom. Expected: sections feel separated and scannable rather than cramped, and both drill-down sections read as clickable from their subtitles alone.
    3. **«Темы» grid** — with roughly 3, then 8, then 30 configured themes: the card grid reflows cleanly, and above 8 the show-all control appears and expands in place.
    4. **Long text** — with a deliberately long theme name and a deliberately long evidence quote: the chip, the topic card and the panel title each truncate on one line with the full text on hover, and the quote clamp is deep enough to be convincing for coaching. **This last point is an open design question — please say if the clamp should be deeper or removed.**
    5. **End-to-end tagging** — configure a theme with a couple of synonyms on a real project, analyse a call whose transcript contains one of them, then check that the theme appears on the call card, on the journal row, in the «Темы» section, and in the exported spreadsheet; then remove it from the call card and re-run the analysis and confirm your manual removal was not undone.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe specific deviations to address before phase complete.</resume-signal>
  <verify>
    <manual>User confirms all five checks, or reports specific deviations to address before the phase is marked complete.</manual>
  </verify>
  <acceptance_criteria>
    - All five checks were presented to the user and their responses recorded in the summary.
    - Any quote-clamp decision is either applied as a single stylesheet value change or recorded as accepted.
    - Any sample-size tuning decision is recorded against the backlog entry rather than applied silently.
  </acceptance_criteria>
  <done>A human has confirmed the phase's visual contract at three viewports and three theme counts, and has watched a real call get tagged, surface on all four surfaces, and keep a manual correction across re-analysis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated contract → frontend types | The committed OpenAPI document drives the generated type surface both repos rely on |
| locale files → DOM | Translated strings, including interpolated ones, render into the UI |
| planning documents → future phases | The API map and backlog steer what the next phase assumes is true |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10-01 | Information Disclosure | a stale committed contract hiding a new endpoint from review | medium | mitigate | Both repos' check scripts fail on any diff, so the contract cannot drift from the code silently; the API map records the new routes with their roles for review. |
| T-10-10-02 | Tampering | translated copy carrying markup | low | mitigate | Locale values are plain strings rendered as React children; the phase introduces no raw-HTML translation rendering. |
| T-10-10-03 | Repudiation | a deferred security finding lost in a plan summary | medium | mitigate | The export formula-safety finding is written into the backlog with a priority rather than left in a summary. |
| T-10-10-04 | Information Disclosure | the API map publishing internal surface detail | low | accept | The map is an internal planning document in a private repository, consistent with existing practice. |
| T-10-10-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; the generation scripts use dependencies already present per 10-RESEARCH.md § Package Legitimacy Audit. |
</threat_model>

<verification>
Backend commands run from `aiPBX_backend`; frontend commands from `aiPBX`.

- `npx jest --config config/jest/jest.config.ts src/shared/lib/i18n/reportsLocaleParity.test.ts` — four-locale key parity
- `npm run openapi:check` (backend) — committed contract matches the code
- `npm run generate:api-types:check` (frontend) — generated types match the contract
- `npm run lint` and `npm test` (backend); `npm run lint:ts` and `npm run test:unit` (frontend) — full phase gates across both repos
- Human verification per Task 3
</verification>

<success_criteria>
- All phase copy resolves in Russian, English, German and Chinese, gated by a test.
- Both repos' contract check scripts pass with no diff and the generated types carry the new shapes.
- The API map and the backlog reflect what shipped and what was deliberately deferred.
- Both repos' lint and full unit suites pass.
- A human has confirmed the visual contract and one real end-to-end tagging round trip, including that a manual correction survives re-analysis.
</success_criteria>

## Artifacts this phase produces

| Artifact | Repo | Kind | Consumed by |
|----------|------|------|-------------|
| Phase copy in `public/locales/{ru,en,de,zh}/reports.json` | aiPBX | i18n | all phase UI |
| `src/shared/lib/i18n/reportsLocaleParity.test.ts` | aiPBX | new gate | future phases adding report copy |
| Regenerated `openapi.json` (both repos) and `src/shared/api/generated/schema.d.ts` | both | contract | frontend type checking, future API work |
| `.planning/intel/API-MAP.md` rows for the new routes and parameters | aiPBX | intel | next phase |
| `.planning/GAPS.md` entries for export formula safety and evidence sizing | aiPBX | backlog | next milestone |
| Human verification record of the five manual checks | aiPBX | sign-off | phase completion |

<output>
Create `.planning/phases/10-speech-analytics-ux-overhaul-operator-drill-down-call-tags-t/10-10-SUMMARY.md` when done.
</output>
