# Deferred items (Phase 11 / plan 11-05)

Out of scope for 11-05 dead-code cleanup — pre-existing or unrelated WIP on working tree:

| Item | Location | Notes |
|------|----------|-------|
| `operator-linebreak` on `??` | `webpack.config.ts` | Unrelated local WIP; not touched by Playground cleanup |
| `import/no-duplicates` | `src/entities/AiModel/...` | Unrelated ModelsPage WIP |
| `operator-linebreak` on `\|\|` | `ErrorBoundary.tsx` | Pre-existing |

Playground-scoped `eslint` + unit patterns: green after Task 2 fixes (`debugFilters` array-type, `Playground.test` import order).
