---
phase: 11-playground-ux-redesign-for-voice-assistant-testing
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/features/PlaygroundSession/ui/DebugSheet/DebugSheet.tsx
  - src/features/PlaygroundSession/ui/DebugPanel/DebugPanel.tsx
  - src/features/PlaygroundSession/ui/StatusBar/StatusBar.tsx
  - src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx
  - src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx
  - public/locales/ru/playground.json
  - public/locales/en/playground.json
  - public/locales/de/playground.json
  - public/locales/zh/playground.json
autonomous: true
requirements: [PG-UX-03]

must_haves:
  truths:
    - "Debug opens as a side sheet/drawer over Call; hidden by default via Open events (D-09, D-10 / PG-UX-03)"
    - "Tokens/latency/VAD/model metrics live in Debug sheet — not in always-on Call chrome (D-12)"
    - "Default event filters match today: transcript, function, response, session, error, vad — excluding raw audio (D-11 / UI-SPEC zero-one-many)"
    - "Mic device select exists only in Debug sheet and is passed into connect(assistantId, micDeviceId) (D-28)"
    - "Desktop Debug drawer width 400px; mobile fullscreen with back closing to Call (D-17, D-19)"
    - "Connection error details remain reachable in Events/Debug (D-39)"
  artifacts:
    - path: src/features/PlaygroundSession/ui/DebugSheet/DebugSheet.tsx
      provides: "Drawer host for DebugPanel + metrics + mic select"
    - path: src/features/PlaygroundSession/ui/DebugPanel/DebugPanel.tsx
      provides: "Reused event list/filters inside sheet"
  key_links:
    - "CallChrome Open events toggles DebugSheet; session hook remains mounted"
    - "StatusBar metrics section rendered inside DebugSheet only"
    - "connect(..., selectedMicDeviceId) when Start fires"
---

<objective>
Deliver Debug as an overlay sheet: reuse DebugPanel filters, move StatusBar metrics off permanent chrome, add power-user mic device select (D-09…D-12, D-28 / PG-UX-03).

Purpose: Completes progressive disclosure after Call chrome (11-01) and Setup (11-02) without restoring resizable dual-pane.
Output: DebugSheet drawer wired to CallChrome; metrics + device select inside; default filters preserved.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-CONTEXT.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-RESEARCH.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-UI-SPEC.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-01-SUMMARY.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-02-SUMMARY.md
src/features/PlaygroundSession/ui/DebugPanel/DebugPanel.tsx
src/features/PlaygroundSession/ui/StatusBar/StatusBar.tsx
src/features/PlaygroundSession/model/usePlaygroundSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DebugSheet drawer + metrics off StatusBar + default filters</name>
  <files>src/features/PlaygroundSession/ui/DebugSheet/DebugSheet.tsx, src/features/PlaygroundSession/ui/DebugPanel/DebugPanel.tsx, src/features/PlaygroundSession/ui/StatusBar/StatusBar.tsx, src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx, src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx</files>
  <read_first>
    - src/features/PlaygroundSession/ui/DebugPanel/DebugPanel.tsx (default Set of categories)
    - src/features/PlaygroundSession/ui/StatusBar/StatusBar.tsx
    - src/features/PlaygroundSession/ui/SetupSheet/SetupSheet.tsx (drawer pattern from 11-02)
    - 11-UI-SPEC.md Debug layout widths
    - 11-CONTEXT.md D-09…D-12
  </read_first>
  <behavior>
    - Debug mode starts closed; Open events opens right drawer 400px / mobile fullscreen
    - Default selected categories exclude audio (D-11)
    - Status/timer remain in Call header; token/latency/VAD/model blocks render inside DebugSheet
  </behavior>
  <action>
    Per D-09, D-10, D-11, D-12: implement DebugSheet (MUI Drawer mirror SetupSheet widths: 400px). Mount existing DebugPanel inside; keep default filter Set(['transcript','function','response','session','error','vad']). Relocate StatusBar dense metrics into DebugSheet as a metrics section (reuse StatusBar internals or extract MetricsSection). Ensure Call chrome never permanently shows those metrics. Header back / Esc closes to Call (D-19). Do not reintroduce react-resizable-panels as default layout.
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=DebugPanel|debugFilters|eventProcessor</automated>
  </verify>
  <acceptance_criteria>
    - DebugSheet exists and opens from Open events
    - Default filters still exclude audio
    - Always-on Call chrome shows status+timer only for connection metrics
    - PlaygroundLayout is not the primary Call layout
  </acceptance_criteria>
  <done>Debug sheet hosts events+metrics; progressive disclosure complete for PG-UX-03 / D-09…D-12.</done>
  <reversibility rating="costly">D-10 Debug-as-sheet replaces permanent split; reverting needs PlaygroundLayout wiring again.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mic device select in Debug + pass device id on connect</name>
  <files>src/features/PlaygroundSession/ui/DebugSheet/DebugSheet.tsx, src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx, src/features/PlaygroundSession/model/usePlaygroundSession.ts</files>
  <read_first>
    - src/features/PlaygroundSession/model/usePlaygroundSession.ts (connect(assistantId, micDeviceId?))
    - 11-CONTEXT.md D-28
    - 11-RESEARCH.md Open Questions mic persistence (session-only)
  </read_first>
  <behavior>
    - Enumerating audioinputs populates a select in DebugSheet
    - Selected device id is session-only state (no localStorage required)
    - Start/connect passes the selected micDeviceId into connect
  </behavior>
  <action>
    Per D-28: add mic device Combobox/Select inside DebugSheet only (power-user). Use navigator.mediaDevices.enumerateDevices after permission. Hold selected device id in V2/orchestrator session state; on Start pass to connect(assistantId, micDeviceId). Do not place device select in Call chrome or Setup. Add a small unit test around the wiring helper or mocked enumerateDevices if practical; otherwise assert connect spy receives device id from a pure selector helper.
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=DebugSheet|PlaygroundSession|usePlaygroundSession</automated>
  </verify>
  <acceptance_criteria>
    - Mic device control exists only under DebugSheet
    - connect is invoked with the selected device id when set
    - lint:ts passes for touched files
  </acceptance_criteria>
  <done>Debug mic device select wired to existing connect signature (D-28).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug event buffer | Client-only; existing caps 500/2000 |
| Mic device enumeration | Browser mediaDevices; session-scoped choice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Information disclosure | DebugPanel event log | medium | mitigate | Keep client-only buffer caps; do not persist events to localStorage/server |
| T-11-01 | Tampering | Event payload render | medium | mitigate | Text-only rendering of event JSON/summary |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- Default filter exclusion of audio still covered by tests or explicit assertion
- Manual checklist item 5: Debug sheet filters + metrics; Call is single column
</verification>

<success_criteria>
- Debug is an overlay sheet with metrics + mic device select
- Call chrome remains status+timer only
</success_criteria>

## Artifacts this phase produces

- DebugSheet
- Metrics relocated into Debug
- Mic device select (session-scoped)

<output>
Create `.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-03-SUMMARY.md` when done
</output>
