---
phase: 11-playground-ux-redesign-for-voice-assistant-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx
  - src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx
  - src/features/PlaygroundSession/ui/CallCenter/CallCenter.tsx
  - src/features/PlaygroundSession/model/playgroundMode.ts
  - src/features/PlaygroundSession/model/playgroundMode.test.ts
  - src/features/PlaygroundSession/model/callCenterState.ts
  - src/features/PlaygroundSession/model/callCenterState.test.ts
  - src/features/PlaygroundSession/model/useMicPermission.ts
  - src/features/PlaygroundSession/model/useMicPermission.test.ts
  - src/features/PlaygroundSession/model/usePlaygroundSession.ts
  - src/features/PlaygroundSession/lib/eventProcessor.ts
  - src/features/PlaygroundSession/lib/eventProcessor.test.ts
  - src/pages/Playground/ui/Playground/Playground.tsx
  - public/locales/ru/playground.json
  - public/locales/en/playground.json
  - public/locales/de/playground.json
  - public/locales/zh/playground.json
autonomous: true
requirements: [PG-UX-01, PG-UX-04, PG-UX-05, PG-UX-06, PG-UX-08]

must_haves:
  truths:
    - "After selecting an assistant the UI is in Call mode; Setup opens only via Open settings (D-01, D-02 / PG-UX-01)"
    - "usePlaygroundSession stays mounted while opening/closing Setup or Debug stubs — call does not drop on mode change (RESEARCH Pitfall 1)"
    - "Permanent Call chrome shows connection status + timer only; dense StatusBar is not always-on (D-12)"
    - "Default layout is Call-first; Debug is closed by default and there is no permanent 65/35 resizable split as primary chrome (D-09, D-10)"
    - "Empty assistants list shows empty-state heading/body + CTA Create assistant (UI-SPEC empty / D-40)"
    - "Idle Call center shows readiness checklist (mic / assistant / model) + Start; previous session appears as collapsed Last test · N turns when history exists (D-13, D-14 / UI-SPEC partial)"
    - "Connecting shows centered progress + Cancel connection; after 18s timeout message + cancel returns to idle (D-41 / UI-SPEC loading)"
    - "While connected, live transcript is the primary center content (D-15 / UI-SPEC populated)"
    - "Post-call shows Test summary strip (duration, errors, tool calls) above frozen transcript (D-16)"
    - "Connection errors use toast + header status; recovery is normal Start from idle (D-39 / UI-SPEC error connection)"
    - "Mic problems surface as red checklist item + Retry check; permanent deny shows short warning + detailed browser instructions in tooltip; Start disabled until mic ok (D-25, D-26 / UI-SPEC error mic)"
    - "Entering Call after assistant select proactively requests mic permission (D-27)"
    - "Mute and volume controls live in Call chrome and affect the audio graph (D-30)"
    - "Mid-call mic lost shows error; user hangs up; fix via checklist after — no silent reconnect (D-29)"
    - "Mobile <900px: sticky bottom Start/Stop bar (56px + safe-area); checklist uses compact chips (D-20, D-21 / PG-UX-05). Fullscreen Setup/Debug sheets land in 11-02/11-03 (D-17 deferred from tracer stubs)."
    - "Onboarding assistants path: Setup/Debug visible but collapsed secondary; assistantId preselect may be changed (D-22, D-23, D-24 / PG-UX-08)"
    - "Playground page still receives DisconnectInfo and fires playground_call_success + first_call when connected ≥10s (PG-UX-08)"
    - "Switching assistant with existing history requires confirm dialog; select disabled while connected (D-37, D-38)"
  artifacts:
    - path: src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx
      provides: "Mode-driven orchestrator with session hook at root"
    - path: src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx
      provides: "Header status+timer, mute/volume, Start/Stop, settings/events entry"
    - path: src/features/PlaygroundSession/ui/CallCenter/CallCenter.tsx
      provides: "Empty / checklist / connecting / transcript / summary states"
    - path: src/features/PlaygroundSession/model/useMicPermission.ts
      provides: "Proactive mic probe + checklist state"
    - path: src/features/PlaygroundSession/model/playgroundMode.test.ts
      provides: "Wave 0 mode transition coverage"
  key_links:
    - "PlaygroundSessionV2 mounts usePlaygroundSession once; mode only toggles overlays"
    - "Playground.tsx onSessionDisconnect ≥10s analytics unchanged"
    - "connect(assistantId) path still owns real getUserMedia; probe stream tracks stopped immediately"
    - "Layout breakpoint uses matchMedia max-width 899px per UI-SPEC, not useDevice alone"
---

<objective>
Prove Call-first Playground chrome end-to-end: mode shell, readiness checklist, connecting timeout, live transcript, post-call summary, mute/volume, mic checklist, mobile sticky bar, and onboarding-safe disconnect analytics — while keeping `usePlaygroundSession` mounted (D-01…D-02, D-09…D-30, D-37…D-41, D-22…D-24).

Purpose: Tracer for PG-UX-01/04/05/06/08 before Setup form or Debug sheet polish. Retires permanent dual-pane as default chrome (D-10 costly — flagged here; full Setup/Debug sheets land in later plans).
Output: Reworked PlaygroundSessionV2 Call surface with stub Setup/Debug entry points closed by default; Wave 0 unit tests for mode/mic/call-center/summary.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-CONTEXT.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-RESEARCH.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-UI-SPEC.md
@.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end Call-first mode shell — session stays mounted</name>
  <files>src/features/PlaygroundSession/model/playgroundMode.ts, src/features/PlaygroundSession/model/playgroundMode.test.ts, src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx, src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx, public/locales/ru/playground.json, public/locales/en/playground.json, public/locales/de/playground.json, public/locales/zh/playground.json</files>
  <read_first>
    - src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx
    - src/features/PlaygroundSession/ui/PlaygroundLayout/PlaygroundLayout.tsx
    - src/features/PlaygroundSession/ui/PlaygroundHeader/PlaygroundHeader.tsx
    - src/features/PlaygroundSession/model/usePlaygroundSession.ts
    - src/pages/Playground/ui/Playground/Playground.tsx
    - .planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-UI-SPEC.md (Layout Contract, Copywriting Contract)
    - .planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-CONTEXT.md (D-01, D-02, D-09, D-10, D-12)
  </read_first>
  <behavior>
    - Mode enum call | setup | debug; selecting an assistant transitions UI to call (D-02)
    - openSetup is a no-op while status is connecting or connected (D-03 gate at chrome level)
    - Opening setup or debug does not remount usePlaygroundSession (same hook instance)
    - Header shows status label/dot + MM:SS timer when connected or post-call of current session; StatusBar is not rendered as always-on chrome (D-12)
  </behavior>
  <action>
    Per D-01/D-02/D-09/D-10/D-12: introduce `playgroundMode` helpers and unit tests (Wave 0). Rewrite PlaygroundSessionV2 so Call is the home surface: replace PlaygroundLayout Group/Panel primary chrome with CallChrome (MUI Button + Lucide icons per UI-SPEC). Keep `usePlaygroundSession({ onDisconnect })` at orchestrator root — overlays for setup/debug may be stub drawers (closed by default) but must not conditionally unmount the session hook or key the tree on mode. Header left→right: AssistantSelect, Open settings / Open events (desktop icon+label; mobile icon-only with aria-label), status+timer, desktop Start/Stop (Stop destructive, immediate hangup), mute/volume placeholders OK if wired in Task 2. Copy from UI-SPEC Copywriting Contract into playground locales (ru/en/de/zh) for chrome strings. Prefer MUI / shared/ui/mui for chrome per D-34 override. Do not add new npm packages.
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=playgroundMode</automated>
  </verify>
  <acceptance_criteria>
    - playgroundMode.test.ts covers call-after-select and Setup blocked while connected
    - PlaygroundSessionV2 no longer uses PlaygroundLayout as the default always-visible dual-pane
    - Debug entry exists but starts closed
    - onSessionDisconnect prop still plumbed from Playground page
    - Chrome copy keys exist in ru, en, de, zh playground.json
  </acceptance_criteria>
  <done>Call-first shell runs with session hook stable across mode toggles (PG-UX-01, D-01, D-02, D-10, D-12).</done>
  <reversibility rating="costly">D-10 removes PlaygroundLayout as primary chrome; restoring dual-pane later means rewiring CallChrome consumers and drawer hosts.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Call center states + mic checklist + mute/volume + 18s connect timeout</name>
  <files>src/features/PlaygroundSession/ui/CallCenter/CallCenter.tsx, src/features/PlaygroundSession/model/callCenterState.ts, src/features/PlaygroundSession/model/callCenterState.test.ts, src/features/PlaygroundSession/model/useMicPermission.ts, src/features/PlaygroundSession/model/useMicPermission.test.ts, src/features/PlaygroundSession/model/usePlaygroundSession.ts, src/features/PlaygroundSession/lib/eventProcessor.ts, src/features/PlaygroundSession/lib/eventProcessor.test.ts, src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx, src/features/PlaygroundSession/ui/ConversationPanel/ConversationPanel.tsx</files>
  <read_first>
    - src/features/PlaygroundSession/lib/eventProcessor.ts
    - src/features/PlaygroundSession/lib/eventProcessor.test.ts
    - src/features/PlaygroundSession/model/usePlaygroundSession.ts (initAudioInput, playAudioChunk, connect)
    - src/features/PlaygroundSession/ui/ConversationPanel/ConversationPanel.tsx
    - src/features/PlaygroundSession/ui/StatusBar/StatusBar.tsx
    - 11-UI-SPEC.md Call center states + Color/Typography
    - 11-RESEARCH.md Pitfalls 4–6
  </read_first>
  <behavior>
    - callCenterState maps idle/connecting/connected/error/post-call to the UI-SPEC center table
    - Connecting timeout fires at 18s then Cancel connection returns idle via disconnect (D-41)
    - useMicPermission: granted → checklist OK; denied → short warning + tooltip instructions + Retry check; Start disabled until OK (D-25, D-26)
    - Proactive probe on entering Call after assistant select stops tracks immediately (D-27)
    - Mid-call mic/track lost: surface error in Call UI; do not silent-reconnect or auto-hangup; user ends call then fixes via checklist (D-29)
    - Summary strip exposes duration, errorCount (error-category events), functionCallCount (D-16)
    - setMuted toggles MediaStreamTrack.enabled; setVolume drives GainNode on playback path (D-30)
  </behavior>
  <action>
    Per D-13…D-16, D-25…D-30, D-39…D-41: implement CallCenter states (empty CTA via getRouteAssistantCreate for D-40; checklist; connecting LinearProgress; live ConversationPanel; post-call summary). Extend eventProcessor with errorCount from error-category events. Add useMicPermission (Permissions API with getUserMedia fallback). Extend usePlaygroundSession with mute + playback GainNode volume; expose setters in return memo. Wire connection errors to react-toastify + header status using UI-SPEC connection error copy. After hangup, restart is normal Start only (D-07) — no restart banner. Spacing/typography follow UI-SPEC tokens (multiples of 4; Body 14/400, Heading 18/600).
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=callCenterState|useMicPermission|eventProcessor</automated>
  </verify>
  <acceptance_criteria>
    - callCenterState.test.ts and useMicPermission.test.ts pass
    - eventProcessor tests assert errorCount increments on error-category events
    - Empty state CTA navigates to getRouteAssistantCreate()
    - Mute/volume APIs exported from usePlaygroundSession and bound in CallChrome
    - Connecting timeout uses 18s per UI-SPEC discretion
    - Mid-call mic-lost path shows error UI and does not call silent reconnect or auto-disconnect (D-29)
  </acceptance_criteria>
  <done>All Call center states and mic/audio controls observable per PG-UX-04/06 and UI-SPEC covered states.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile sticky bar, onboarding chrome, assistant switch confirm</name>
  <files>src/features/PlaygroundSession/ui/CallChrome/CallChrome.tsx, src/features/PlaygroundSession/ui/CallCenter/CallCenter.tsx, src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx, src/pages/Playground/ui/Playground/Playground.tsx, public/locales/ru/playground.json, public/locales/en/playground.json, public/locales/de/playground.json, public/locales/zh/playground.json</files>
  <read_first>
    - src/pages/Playground/ui/Playground/Playground.tsx
    - src/features/PlaygroundSession/ui/PlaygroundSessionV2/PlaygroundSessionV2.tsx
    - 11-UI-SPEC.md mobile column + Copywriting confirm strings
    - 11-CONTEXT.md D-17…D-24, D-37, D-38
  </read_first>
  <behavior>
    - At max-width 899px Start/Stop live in sticky bottom bar; header keeps assistant + status/timer (D-20)
    - Checklist renders as compact MUI Chips on mobile (D-21)
    - Onboarding query keeps Setup/Debug as secondary subdued controls (D-22, D-23)
    - assistantId URL preselects but remains changeable (D-24)
    - Switch with transcript history opens MUI Dialog confirm; connected disables select (D-37, D-38)
    - DisconnectInfo still reaches page handler (PG-UX-08)
  </behavior>
  <action>
    Per D-17…D-24, D-37, D-38, PG-UX-05/08: use MUI useMediaQuery or matchMedia('(max-width: 899px)') for layout (avoid useDevice alone — RESEARCH Pitfall 8). Sticky bar height 56px + safe-area. Add MUI Dialog for switch confirm with UI-SPEC copy. Preserve Playground.tsx MIN_CONNECTED_MS=10000 analytics path unchanged. Add a focused unit/component test that mocks session disconnect and asserts the page callback still receives DisconnectInfo (Wave 0 PG-UX-08). Landscape ≈ narrow desktop is acceptable as CSS (D-18). System back / header arrow for sheets can be stubbed for Setup/Debug until plans 02–03 if drawers are stubs — document TODO only inside Setup/Debug plan files, not here.
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=Playground|playgroundMode|callCenterState</automated>
  </verify>
  <acceptance_criteria>
    - Mobile sticky bar present under &lt;900px breakpoint
    - Confirm dialog keys exist in four locales
    - Assistant select disabled when status is connected
    - Playground disconnect analytics gate still uses 10_000 ms
    - lint:ts clean for touched files
  </acceptance_criteria>
  <done>Mobile + onboarding + switch confirm ship on Call chrome (PG-UX-05, PG-UX-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Playground WS | Existing socket auth; no new endpoints |
| Browser ↔ Assistants REST | Existing RTK; select only tenant assistants |
| Mic permission UX | User-granted media; copy must not spoof OS dialogs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | ConversationPanel / events | medium | mitigate | Render transcript/events as React text; never dangerouslySetInnerHTML |
| T-11-02 | Spoofing | Mic checklist copy | low | mitigate | Neutral product copy only; MUI Tooltip for instructions (D-26, D-43) |
| T-11-03 | Information disclosure | onboarding analytics | low | accept | Existing first_call/playground_call_success; no new PII in events |
| T-11-SC | Tampering | npm installs | high | mitigate | No new packages this phase — reuse @mui/material already installed |
</threat_model>

<verification>
- Targeted Jest patterns for playgroundMode, callCenterState, useMicPermission, eventProcessor pass
- Manual smoke: select assistant → Call; Start; transcript; Stop → summary (cite 11-RESEARCH checklist 1–3)
- Playground page disconnect analytics path unchanged
</verification>

<success_criteria>
- Call-first IA observable without permanent dual-pane default (D-10)
- Checklist, connecting 18s, transcript, summary, mute/volume, mic deny UX all present
- Onboarding ≥10s analytics contract preserved
</success_criteria>

## Artifacts this phase produces

- CallChrome + CallCenter UI
- playgroundMode / callCenterState / useMicPermission helpers + tests
- Mute/volume extensions on usePlaygroundSession
- Four-locale chrome keys for Call surface

<output>
Create `.planning/phases/11-playground-ux-redesign-for-voice-assistant-testing/11-01-SUMMARY.md` when done
</output>
