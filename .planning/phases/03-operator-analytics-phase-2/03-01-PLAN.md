---
phase: 03-operator-analytics-phase-2
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
must_haves:
  truths:
    - "Dashboard Builder custom metric widgets show real aggregated values"
    - "Boolean/number/enum custom metrics map to stat-card, bar, pie widgets"
  artifacts:
    - path: src/features/OperatorAnalytics/ui/DashboardBuilder/useWidgetData.ts
      provides: customMetricsAggregated wiring
---

<objective>
Wire `dashboardData.customMetricsAggregated` into Dashboard Builder `useWidgetData` (GAP-12).
</objective>

<tasks>
<task type="auto">
  <name>Implement custom metric extraction in useWidgetData</name>
  <files>src/features/OperatorAnalytics/ui/DashboardBuilder/useWidgetData.ts</files>
  <action>
Replace TODO stub with logic mirroring OperatorDashboard custom metrics:
- Lookup metric schema from project.customMetricsSchema by widget.metricId
- Read customMetricsAggregated[metricId]
- Map boolean/number → stat-card value; enum distribution → bar/pie
  </action>
</task>
<task type="auto">
  <name>Unit tests for extractCustomWidgetData</name>
  <files>src/features/OperatorAnalytics/ui/DashboardBuilder/useWidgetData.test.ts</files>
  <action>Add tests for boolean %, number avg, enum distribution pie/bar.</action>
</task>
</tasks>
